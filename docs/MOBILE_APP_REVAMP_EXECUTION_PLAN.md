# Mobile App Revamp — Execution Plan

Status: implementation in progress; local IAP foundation and cross-project validation complete, audited correctness/security/reliability and activation milestones pending, Phase 3 store execution owner-gated, and final-release production/device evidence owner-gated

This file is the execution tracker for `MOBILE_APP_REVAMP_STRATEGY.md`. It incorporates the repository audit and an independent Claude review/reconciliation pass completed on 2026-08-04.

## Product decisions and non-negotiable scope

- [ ] Remove Stripe and RevenueCat from the product and repository.
- [ ] Support paid entitlements only through Apple App Store and Google Play IAP.
- [ ] Do not migrate, grandfather, refund, or preserve existing real paid subscriptions. The owner confirms that the active records found in production are store-approval test accounts, not paying customers; those test accounts must remain covered by the store-test workflow and must not be treated as customer migration data.
- [ ] Disable web premium checkout when Stripe is removed. The web may consume an account entitlement granted by a mobile store purchase, but it must not initiate a web payment.
- [ ] Do not make unrelated revamp work a prerequisite for payment cutover unless it is a security or correctness blocker.

If the web is expected to keep selling premium, stop and change the first decision: that would require retaining a web payment provider and is incompatible with “remove Stripe completely.”

## Review outcome

The strategy is directionally feasible, but its original ordering is not executable. The critical correction is to build and prove native IAP before deleting the current providers. Claude agreed with the audit and added these gates:

1. “No paying users” must cover active, past-due, grace-period, comped/manual/lifetime entitlements, and any web premium state—not only a count of currently paying rows.
2. Web monetization must be an explicit product behavior, not an accidental consequence of deleting Stripe.
3. Apple and Google notification handling must be idempotent from its first implementation.
4. Sandbox/internal-track tests do not prove production notification configuration. Prefer one refundable production smoke transaction per store; an owner-approved waiver must record the remaining risk.
5. Provider schema/data cleanup and temporary provider-code rollback safety are separate concerns. With zero entitlements, the schema can be cleanly replaced after the zero-state gate; old provider code may remain disabled behind a short-lived rollback flag until the first IAP release is stable, then must be deleted.

## Evidence baseline

The following current repository facts drive the remaining order of work:

- First-launch and returning-user routing is implemented and tested, but neither Apple Authentication nor Google Sign-In is installed; social identity must follow the auth/session hardening milestones.
- Expo SDK 54 now includes direct `expo-iap` purchase/restore code, a tested native paywall, Apple/Google verification and notification ingestion, and store-neutral entitlement state. Store sandbox/production configuration and proof remain Phase 3 owner gates.
- RevenueCat and Stripe code and dependencies deliberately remain as a flag-gated rollback surface. Phase 4 must prove zero real entitlements, drain or discard legacy jobs, disable every legacy route/worker, and validate native IAP before deleting that surface.
- The native paywall is still settings-owned, while dashboard and hard-limit upgrade routes navigate away from the user's task. Point-of-intent attribution, context preservation, dismissal telemetry, and server-owned limit usage remain activation work.
- Quiz answer persistence and the new flashcard flow have local completion evidence, but the ordinary quiz route still owns an indefinite client loop and lacks a finite server-derived session summary.
- Progress responses now expose fresh per-wordlist due counts, but the dashboard does not render them, make practice primary, or reconcile app badges.
- Typed privacy-safe activation, IAP, notification-open, identify, and reset events exist, but paywall dismissal/context, push-permission, finite-session, flashcard, voice, and production dashboard evidence remain incomplete.
- The exact API matrix, full lint/vet/format checks, mobile Jest/type/lint/export, web lint/format/build, and Android Maestro suite pass locally. Repository-wide coverage remains below the fixed 70% unit/80% integration policy and is an executable test-coverage milestone, not an owner decision.
- The Appendix C reconciliation confirms additional reachable correctness, authentication, upload, cache, health/readiness, asynchronous-lifecycle, migration, and deployment defects. The strategy coverage catalog below owns each finding explicitly instead of allowing broad workstream checkboxes to imply completion.

## Progress legend

- `[ ]` not started
- `[-]` in progress
- `[x]` complete with evidence linked in the notes
- `[b]` blocked; record the owner and decision needed

## Development protocol

This protocol is adapted from the completed `calculadora-price-sac/mobile/FEATURE_ROADMAP.md` workflow. It is the required workflow for each implementation item in this plan. Work phases and dependencies remain strictly ordered; once an item passes its gates, the next unlocked item starts automatically without waiting for another user message.

Every Claude review starts with the model order `fable` → `opus` → `sonnet`. Try `fable` first on every new prototype or implementation review, even if it was unavailable during an earlier review; fall back to `opus` only when that attempt is temporarily unavailable, then to `sonnet` only when `opus` is also temporarily unavailable. This fallback applies only to the current review attempt and never changes the preferred order: a later attempt must start from `fable` again because a temporarily unavailable model may have recovered. A model's substantive review findings are not an availability failure and must be reconciled with that model rather than triggering fallback. Record every attempted model and the model that completed each review in the item evidence.

For every item, in order:

1. **Create a UI/UX prototype when needed.** Before implementing any UI-bearing item, write a short design brief from the acceptance criteria and build one or more disposable HTML/React mockups under `docs/mockups/<item-id>/`. Keep prototypes isolated from production dependencies and use real copy, representative data, and mobile viewports.
2. **Iterate the prototype with Claude.** Review the mockup for hierarchy, interaction states, accessibility, content, responsive behavior, and consistency with the existing app. Revise until Codex and Claude agree there is no material objection against the defined checklist, for up to three review/revision rounds. Record the chosen variant, rejected alternatives, and final rationale in `docs/mockups/<item-id>/DECISION.md`. No additional user sign-off is required for an ordinary in-scope design choice; unresolved disagreement after three rounds becomes `[b] BLOCKED — owner decision`. Temporary Claude capacity or model availability follows the automatic polling protocol below and is not a blocker. Missing authentication or another failure with no viable retry path becomes `[b] BLOCKED — owner action: restore Claude CLI access and rerun the design review`; never treat an unreviewed prototype as approved.
3. **Write tests first.** Add a failing unit, integration, contract, or component test that reproduces the bug or specifies the behavior. For store integrations, use provider fixtures/mocks and test redelivery, invalid data, and out-of-order events without live purchases. Never weaken or delete an existing test to make the change pass.
4. **Implement to green.** Keep the change within the item’s stated scope. Update API contracts, migration notes, and user-facing copy in the same item when they are part of its acceptance criteria.
5. **Ask Claude to review the implementation diff.** Run an adversarial review before marking the item complete, with the item ID, approved mockup decision, and this plan as context. Address every finding that is real; record disagreements and their evidence in the completion note. Re-request review after substantial rework.
6. **Run the complete applicable test suites.** At minimum, API changes run the relevant Go unit/integration tests and coverage target; mobile changes run `npm test`; web changes run the web test/build checks available in the repository. Run cross-package tests when a shared contract or package changes.
7. **Run UI validation for UI changes.** Use Maestro on Android for affected mobile flows, with a scoped flow plus a fixed smoke subset when app-open behavior or shared surfaces change. Add or update the flow in the same item. Do not claim iOS/store validation from Android evidence. A single full Maestro suite is the final gate after the last UI item, before release-owner actions; fix regressions and rerun affected flows to green.
8. **Run static checks immediately before commit.** Use the current repository commands: API `make format-check`, `make lint`, and relevant tests; mobile `npm run typecheck`, `npm run lint`, and formatter checks; web `npx eslint .`, `npm run format:check`, and `npm run build` when applicable. Re-check generated migrations and `git diff --check`.
9. **Commit locally, one commit per item.** Use a concise imperative subject and include the roadmap checkbox/completion note in that commit. Do not push, open a PR, publish a release, change production data, or submit store builds unless the user explicitly authorizes that separate action.
10. **Close the item only after steps 1–9 pass.** Change its checkbox to `[x]` and append a dated evidence note listing tests, static checks, prototype/Claude decisions, implementation review, and any remaining owner gate. Then proceed immediately to the next unlocked item.

### Autonomous progression rule

The agent may continue through all locally executable milestones without pausing for user confirmation, including entitlement design, provider code, sandbox fixtures, and reversible local schema/code changes. It must pause before any production migration application—additive or destructive—because live schema changes can affect availability and workers, as well as before production data mutation, store configuration/submission, release publication, or any other irreversible external action. It must also pause the dependent branch if a required reviewer has no viable retry path for a consensus-gated UI decision, for a missing credential, or for a contradiction defined as conflict with an explicit user decision, this plan, repository instructions, or an accepted `DECISION.md`—not a stylistic disagreement. Temporary reviewer capacity keeps the item active under the polling protocol while independent work continues. A normal design choice within the roadmap is resolved through the Codex/Claude prototype loop and recorded rather than escalated.

### Pending and blocker protocol

- **Pending (`[ ]`)** means the item has not started or is waiting on an earlier dependency; it is not an incident.
- **In progress (`[-]`)** means implementation or validation is actively underway.
- **Blocked (`[b]`)** means a specific external state, contradiction, approval, or unavailable required tool prevents completion; it must include the date, evidence already produced, exact owner action, and the condition that will make the item runnable again.
- Autonomous work is bounded at one milestone item per lane: no bundling multiple roadmap items into one commit, and no starting the next item until the current item has its tests, reviews, validation, and evidence note complete. This milestone cadence replaces routine user check-ins.
- When a blocker appears, complete every automatable part first: tests, local implementation, mockups, migrations drafted but unapplied, reports, and handoff instructions. Do not fabricate the missing live evidence or weaken acceptance criteria.
- A temporary reviewer/tool limit with a provider-supplied reset time is a retry condition, not a terminal goal blocker. Keep the goal active, record the reset timestamp as advisory rather than authoritative, and start a yielded polling loop attached to that active goal with `docs/poll-claude-review.sh <prompt-file> <result-file> <repository>`, keeping its result, attempt log, and lock under `/tmp` rather than the worktree. Every poller prompt must require a terminal `## APPROVED` or `## CHANGES REQUIRED` marker; the helper persists and resumes only when the final nonblank output line is exactly one of those markers, so exit-zero availability, usage, credit, authentication, or other non-review output cannot become evidence while a genuine review may freely discuss those failures. Each cycle executes three distinct bounded attempts in the order `fable` → `opus` → `sonnet` because a shared-session-limit error may bypass the CLI's built-in model fallback; it stops on the first substantive review, but never treats an availability error as review evidence. Probe once per minute for the first five minutes, then every five minutes while independent work exists, emit a concise heartbeat at least every 60 seconds during both calls and waits, and continue probing even before a stated reset because capacity can recover early. Automatically rerun the actual blocked command rather than a status-only check. Do not end the turn, wait for Lucas to announce recovery, or substitute a passive reminder while an automatic retry path exists; if the execution session is interrupted, the next automatic continuation must first re-check the result and attempt log, restart the helper when needed, and resume the same milestone. Every resumed Claude review still starts at `fable`; successful availability does not waive reconciliation or validation. Mark the goal blocked only when no reset/retry path exists or repeated post-reset cycles fail and no independent work remains.
- A blocked sub-item does not stop independent work. Continue the next milestone whose dependencies are satisfied; pause only the blocked dependency branch and any items that genuinely depend on it.
- When external state changes, re-check it read-only, rerun the previously blocked validation, obtain the required Claude/design review, and replace `[b]` with `[x]` only after the normal completion gates pass.
- New findings are recorded in the relevant workstream and triaged as launch-blocking, fix-before-release, or deferred; they do not silently expand the current item.
- Business classification decisions—such as whether an entitlement is genuinely a customer or an approved test account—route to Lucas; Codex and Claude may collect evidence but must not classify ambiguous records autonomously.
- The status report should always state: current milestone, completed evidence, active blockers, independent work continuing, exact owner actions, and the next automatic step after unblocking.

### UI/UX prototype acceptance

- Prototype screens cover the happy path plus loading, empty, error, disabled, pending, success, and back/navigation states relevant to the item.
- Prototypes are checked at 390×844 and 430×932 phone widths, must meet WCAG 2.2 AA contrast and applicable keyboard/screen-reader semantics, and show the actual copy hierarchy before production implementation; a failed applicable check blocks approval unless an explicit `[b]` owner exception is recorded.
- HTML/React prototypes run an automated accessibility/contrast check with `axe` or an equivalent tool, and the tool/version/result is recorded; visual review alone is not sufficient evidence.
- `docs/mockups/<item-id>/DECISION.md` includes the selected variant, rejected alternatives, Claude review outcome, unresolved limitations, acceptance states checked, and the production files expected to change.
- Prototype approval does not replace Expo implementation tests or Maestro validation; it is the design gate that makes those implementation decisions explicit.

### Animation protocol

Animations are reviewed as behavior, not inferred from screenshots. Any item introducing motion must include an animation spec in its `DECISION.md` covering:

- trigger and user purpose: feedback, navigation, loading, celebration, or state transition;
- duration, delay, easing, properties, repeat count, and interruption/cancellation behavior;
- reduced-motion behavior and a non-animated fallback that preserves comprehension and access to the action;
- loading/error/offline behavior, unmount cleanup, and whether motion can block input;
- performance budget and the chosen implementation: Reanimated for interactive, gesture-driven, spring, scroll, and interruptible motion; the existing native-driver `Animated` API only as a temporary compatibility path while a touched surface is migrated; and Lottie only for finite decorative illustrations that justify the asset cost. State whether each animation is new or a migration of existing `Animated` code, and do not leave both systems driving the same component.

For each animated UI prototype:

1. Implement the motion first in the HTML/React mockup with CSS transitions/keyframes or a small state machine to explore timing and hierarchy; capture a short screen recording or frame sequence at both phone widths.
2. If the motion is interactive, gesture-driven, spring-based, scroll-driven, interruptible, or platform-sensitive, build a small Expo/Reanimated spike before approval. CSS alone is not evidence that native motion will feel or perform correctly.
3. Ask Claude to review the prototype and, where applicable, the Expo spike for timing, hierarchy, motion meaning, interruption, reduced-motion fallback, and whether the animation is helping or distracting; revise within the same three-round consensus limit. If consensus is not reached after three rounds, stop the item at `[b] BLOCKED — owner decision` with the competing options and evidence; do not implement an unresolved motion choice autonomously.
4. Implement the approved behavior in Expo with deterministic state tests, reduced-motion tests, cleanup/unmount coverage, and no animation-dependent assertions in Maestro; Maestro should wait for the resulting accessible state instead.
5. Check the affected flow on the Android emulator for visible jank, input lock, layout shift, and completion-state correctness; this is not evidence of iOS parity. Before an interactive, gesture-driven, spring, or haptic animation ships, require a physical-device pass on both iOS and Android for gesture latency, jank, interruption, and haptic fallback. If the devices or credentials are unavailable, finish all automatable checks and mark only the release gate `[b]` with the exact owner action.

Animation acceptance requires that the action and its result remain understandable with motion disabled, repeated taps do not create duplicate transitions, interrupted animations settle into a valid state, and the implementation does not introduce an obvious frame-rate or startup regression. The Claude diff review and static validation must also identify the animation system used by each touched component and fail the item if one component is driven by both legacy `Animated` and Reanimated.

The motion toolbox is selected by behavior, not novelty:

- Reanimated plus Gesture Handler for native, interruptible transitions, drag/swipe gestures, springs, scroll-linked effects, and shared-element-like choreography. `react-native-reanimated` 4 is now a direct, validated dependency from the primitive/flashcard work; reuse it without another spike for non-gesture motion. Gesture Handler is not yet direct, so the first gesture-driven item must add it, provide the root wrapper, and confirm Expo 54/RN 0.81.5 New Architecture compatibility before feature work proceeds.
- SVG or ordinary React Native views for lightweight programmatic shapes, loaders, progress, and feedback that should remain themeable and accessible.
- Lottie for finite, authored vector illustrations such as onboarding or completion moments; keep the asset local, bounded, cancellable, and replaceable by a static fallback. The app already has `lottie-react-native`, but each asset still needs a bundle-size and reduced-motion review.
- Image generation for visual exploration, custom static illustrations, texture/background concepts, and storyboard frames that help choose motion. Generated raster art is not treated as an interactive animation implementation or as a substitute for a tested native animation asset.
- Haptics and sound only as optional reinforcement of a visible state change, with permission/platform checks and a silent fallback.

### Blocked protocol

When an item needs owner-only access—production subscription data, App Store Connect, Google Play Console, Apple/Google credentials, a real device, store review, PostHog, or a paid external service—complete every automatable part first. Mark the item `[b] BLOCKED — owner action: <exact steps for Lucas>` and list the evidence already produced. Never fabricate live purchase, notification, store-review, production, or analytics evidence.

### Scope and worktree rules

- Keep the IAP critical path sequential: entitlement contract → Apple/Google implementation → store test gate → cutover → provider removal.
- Use parallel lanes only after Phase 0 and the Phase 0.5 implementation-readiness gate are complete, and only with an explicit disjoint write scope. The Calculadora site-track exception is not automatically applicable here.
- Before starting parallel work, record each lane’s owner, worktree/branch, allowed paths, dependency gate, and merge order in the milestone note. No two lanes may edit the same production file, migration, shared contract, or roadmap section.
- Lanes that need database migrations coordinate sequence numbers through one integration owner; lanes that share an API contract must merge the contract first and run integration tests before either lane is marked complete.
- A parallel lane is complete only after its changes are integrated into the execution branch and the combined validation passes; isolated worktree tests alone are insufficient.
- Preserve unrelated working-tree changes, including the existing untracked `audit.txt`; do not reset, clean, or overwrite them.
- Historical migration files remain immutable. Add forward migrations and record rollback/backup expectations rather than rewriting migration history.

### Parallelization decision and lane matrix

The plan is intentionally hybrid rather than fully sequential:

| Lane | Can start after | Scope | Must wait for |
|---|---|---|---|
| Critical path | Phase 0 → 0.5 implementation readiness | Entitlement contract, IAP backend/mobile implementation, store validation, cutover/removal | Each prior gate; never skip the store gate before provider removal |
| Activation UX | Phase 0.5 implementation readiness | Onboarding, starter content, word entry, quiz/session loop, streaks, due/practice UX, mockups and Maestro flows | Store contract only for purchase/paywall surfaces; shared auth/subscription files require coordination |
| Reliability | Phase 0.5 implementation readiness | Scoped correctness, security, timeout, lookup, worker, and idempotency fixes | Any shared repository/service file already owned by another lane |
| Web | Phase 0 | Web checkout-disabled behavior, entitlement display contract, web validation | Final entitlement API shape before consuming mobile-store entitlements |

Within Phase 2, Apple and Google provider implementations may proceed in parallel after the store-neutral contract is reviewed. Mobile purchase UI may proceed against mocked contract fixtures, but integration cannot be marked complete until the backend contract and provider verification tests pass.

The following remain sequential and cannot be parallelized: zero-entitlement classification, activation baseline definition, entitlement contract approval, production/store validation, live cutover, and Stripe/RevenueCat deletion.

### Final release gate

After all code items are complete, run the full applicable API, mobile, and web validation, the full Maestro suite, and the production-readiness checks in Phase 3. Owner-only store configuration, production smoke purchase, release submission, and live monitoring remain `[b]` until the owner performs them and records the evidence.

Final local UI validation progress:

- [x] **2026-08-06 — full Android Maestro suite complete.** Provisioned fresh local-only Postgres/Redis/MinIO state, applied migrations through `000076` plus River migrations, started the development API and Metro, booted the Android 34 `Maestro_ANDROID_pixel_6_android-34` AVD, and ran the complete checked-in suite together. `first-launch-routing.yaml` and `flashcard-completion.yaml` both pass in the final rerun (`2/2` flows in 1m38s). Added an idempotent local fixture script that hashes the development test password through stdin, seeds one completed flashcard without calling OpenAI or sending email, never prints credentials, and succeeds on repeated runs. The flashcard flow now cold-starts and uses the app's canonical `__DEV__` credential prefill instead of cursor-dependent text erasure, which had left a stale email suffix and caused a false login failure. Fable audited the current schema, fixture transaction/idempotency, credential boundary, flow determinism, README, and evidence scope, then returned `APPROVED`; after its process-list hardening suggestion was adopted, the same Fable reconciliation returned `APPROVED`. Bash syntax, two consecutive seed runs, TypeScript, repository-wide mobile lint, and all 33 Jest suites/249 tests pass. This evidence covers the Android emulator only; physical iOS/Android animation checks and store builds remain owner-gated.
- [x] **2026-08-06 — final cross-project local validation complete.** Mobile lint, TypeScript, all 33 Jest suites/249 tests, Android production export, and the full Android Maestro suite pass. Web now exposes a working Next 16 `npm run lint` script; lint, Prettier, the four account-deletion contract tests, the 69-page production build, and sitemap generation pass. The exact API `make test` runs the complete containerized race-enabled integration matrix and produces nonempty coverage artifacts; `make lint`, an uncapped full-tree lint run, `make format-check`, `go vet ./...`, the race-enabled unit package, focused validator tests, and diff checks pass. This is local evidence only and does not clear aggregate coverage policy, physical-device, sandbox/store, production analytics, retention-sign-off, or release gates.

## Authoritative strategy coverage and milestone catalog

This catalog is the single source of truth for remaining implementation work from `MOBILE_APP_REVAMP_STRATEGY.md` priorities 1–25 and Appendix C. A broad phase or historical workstream note cannot close a row here: each pending or partial row closes only through its allocated terminal milestone and the evidence required by that milestone.

Structural audit on 2026-08-06: the strategy exposes exactly 25 numbered priorities and 72 Appendix C rows; the status tables below contain exactly 25 and 72 corresponding rows. The traceability allocation may group adjacent IDs only when they share the same terminal milestone, but it may not omit or implicitly close any source row.

Catalog audit on 2026-08-06: the dependency catalog defines 60 unique milestone IDs, all 60 have an explicit acceptance boundary, every exact milestone reference resolves to one of those IDs, and no duplicate ID exists. The 96 explicit dependency edges are acyclic; `PHASE4-CUTOVER` has 22 enumerated direct prerequisites, exactly matching all 22 other milestones whose class contains `CUTOVER`, so the final provider-removal gate neither omits a classified blocker nor inherits unrelated `REVAMP`/`NEXT` work. Natural-language gates such as the existing Phase 2 contract and physical-device evidence remain deliberately external dependencies rather than fake milestone IDs.

- `MAP-1`: complete locally on 2026-08-06. `docs/validate-mobile-revamp-plan.sh` proves all structural checks, one terminal allocation for every priority and Appendix row, one evidence boundary for every catalog ID, valid dependency references, an acyclic graph, and exact Phase 4/CUTOVER parity. Fable independently reconstructed and spot-checked the 25 priorities, 72 findings, 60 milestones, 96 dependency edges, and shutdown evidence; mutation-tested the validator; and found one false-review hazard in the unattended polling helper. Reconciliation replaced the weak failure blocklist with an exact final-nonblank verdict contract, proved poisoned-result recovery and genuine-review acceptance through a fake CLI, lengthened repository-wide attempts without changing model order, and added an immediate retry-wait heartbeat so every call/wait transition remains under 60 seconds. The same Fable-first reconciliation returned `APPROVED` after `bash -n`, the full validator, fake-CLI, resume, and diff checks passed. Stale broad-workstream semantics are marked historical, current source claims are refreshed, and this catalog remains the single tracker so implementation state, acceptance evidence, and owner gates cannot drift.

| Priority | Current status | Evidence already present | Missing work that must be tracked |
|---|---|---|---|
| 1 Front door and social auth | Partial | First-launch/onboarding and returning-user routing implemented and tested | Neither Apple Authentication nor Google Sign-In is installed; only generic `expo-auth-session` remains. Add Apple and Google sign-in, backend token exchange, account linking, platform/store compliance, and funnel evidence. |
| 2 Point-of-intent paywall | Partial | Native IAP paywall and settings entry implemented | `NativeIAPPaywall` is still imported only by Settings and dashboard upgrade routes still push `/settings`. Add a global paywall provider, contextual trigger attribution, in-place limit/feature/session-end entry, dismissal analytics, and verified store intro-offer rendering. |
| 3 Funnel instrumentation | Partial | Typed privacy-safe activation, IAP, notification-open events and identify/reset behavior | No PostHog feature-flag use exists, and the typed catalog still lacks paywall dismissal, upgrade/limit/feature-gate attribution, push permission, flashcard, and voice-session events. Add those boundaries plus production dashboard/export evidence. |
| 4 Starter content and interactive onboarding | Pending | Existing onboarding/routing only | Curated starter packs, pre-auth static interactive demo, language/level/goal personalization, shortened flow, time-to-first-quiz evidence |
| 5 Quiz-session ending | Partial | Client-owned summary contract and flashcard completion implemented | `quiz.tsx` still increments local counts and `handleNextQuiz` indefinitely; the progress component still receives accuracy counts. Add finite quiz sessions, completion summary, score/box movement/streak/next action, celebration/reduced motion, and contextual paywall/review rotation. |
| 6 User streak and daily goal | Pending | Existing per-wordlist analytics plus an unused historical SQL user-streak function | Dashboard still takes the maximum per-wordlist streak and hides zero; no goal fields or UI exist. Add server-owned cross-wordlist streak, timezone/day semantics, daily goal, freeze/repair policy, milestones and UI. |
| 7 Surface SRS | Partial | Fresh backend due count and notification deep-link routing | `dueCount` is consumed into a map but not rendered; app badges are explicitly disabled and dashboard remains list/create oriented. Add due badges, dashboard primary practice CTA, app badge lifecycle, and notification-to-session measurement. |
| 8 Design system and tabs | Partial | Button/Card/Input/Sheet/UiText primitives and semantic tokens | `mobile/app` contains only a flat stack and no tabs route group. Add Badge/AppModal/ScreenHeader consolidation, bottom tabs, touched-screen migrations, dead token cleanup, and full dark-mode/typography propagation. |
| 9 Delight layer | Partial | Reanimated foundation, paywall and flashcard motion | Neither `expo-haptics` nor `expo-image` is installed, sound remains disabled, and core quiz controls remain legacy touchables. Add quiz answer feedback, haptics/sound policy, press primitive migration, quiz skeleton/image caching, session/streak celebration, and physical-device validation. |
| 10 Progressive limits and gating | Pending | Existing server limit enforcement and native paywall | The client still hardcodes 10 words, closes the wordlist modal before upsell, hides the quiz selector for free users, and leaves limits undisclosed. Add server-exposed limit usage, 8/10 nudge, context-preserving 11th-word paywall, honest onboarding caps, and visible locked feature previews. |
| 11 US iOS purchase path | Locally complete, externally gated | One native IAP path per platform replaces reachable Stripe/RevenueCat routing | Phase 3 Apple store proof and live release evidence |
| 12 Win-back and offers | Pending/external dependency | Store offer metadata boundary exists | Product policy, Nth-dismissal experiment, store-configured win-back/promotional offers and code redemption; no invented discounts |
| 13 Bulk entry/import | Pending | No reachable batch API or mobile bulk flow found | Add paste-list within limits, an idempotent batch API/UX, CSV/Anki import, and a separately approved premium OCR decision with privacy/cost controls. |
| 14 Notification repertoire | Pending | Daily/due backend pipeline, receipts, routing, and open tracking | Backend queries still recognize only daily/due reminder types, no Android channel setup exists, and client sound remains disabled. Add user schedule/preferences, permission timing, channels, streak/enrichment/recap/win-back types, cap policy, and offline fallback. |
| 15 Widgets/share cards | Pending | No widget/share dependency or app surface found | Add platform feasibility spikes, due/streak data contract, deep links, share-card design/export/privacy, and physical platform validation. |
| 16 Free analytics teaser | Pending | Analytics route/screen exists | `WordlistItem` rejects every free analytics tap before navigation. Add current-week free view, visible locked historical/advanced previews, and Progress-tab entry. |
| 17 Silent free-tier degradation | Pending | Limit service exists | Offline controls still return `null` for free users; free analytics use a 15-minute client freshness window and one-hour backend TTL versus ten seconds/one minute for premium, without disclosing staleness. Add explicit enrichment/offline/TTL/session-staleness states with retry or contextual upgrade paths. |
| 18 Accessibility/dynamic type | Partial | New primitives and touched flashcard/paywall surfaces include a11y/reduced-motion work | Screen-by-screen audit, localized labels, font-scale conventions, automated checks and physical-device validation |
| 19 Performance/code health | Partial | Some touched flows decomposed/fixed; routing and test harness repaired | Measured style/render cleanup, remaining JS-thread animation issues, god-file decomposition as touched, URL/magic-limit defects, regression budgets |
| 20 i18n/docs hygiene | Pending/partial | New copy localized across eight locales in completed items | Current locale scalar counts differ (English 840 versus 820-828 elsewhere) and roughly 182 TypeScript call sites still use inline translation defaults. Add automated locale parity/orphan/missing-key checks, remove inline defaults, prune dead copy, and rewrite design-system documentation. |
| 21 Native IAP replacement | Partial/external gate | Store-neutral backend, Apple/Google verification/events, mobile purchase UI, local tests | Gate 2 policy, Gate 3 stores, Phase 4 cutover/provider deletion, web disabled state, live monitoring |
| 22 Critical backend correctness | Partial; reachable defects confirmed | Several scoped lookup, transaction, worker, webhook, deletion and provider issues fixed; owned wordlist updates and presence-aware owned word updates now have full PostgreSQL contracts | Case-insensitive login is not backed by normalized uniqueness; HTTP not-found checks outside the repaired word-update route still compare `errors.Is` to a fresh pointer and misclassify errors; error-report regeneration still deletes learning-linked definitions and suppresses some transactional substep/commit failures. Continue closing every reachable Appendix C launch defect and prove legacy-only defects disappear at Phase 4 rather than rebuilding removed providers. |
| 23 Backend security | Partial; reachable launch blockers confirmed | Sentry/log PII controls, distractor scoping, and provider startup/auth hardening | Authentication still verifies with `os.Getenv("JWT_KEY")` per request through deprecated `dgrijalva/jwt-go`; the only empty-key check occurs lazily during token issuance, tokens still last one year with email/plan claims, and no refresh/revocation exists. Auth routes have no limiter; profile upload still accepts unbounded JSON/base64 plus caller-controlled extension; static auth uses ordinary string comparison; security headers are absent; production cookie age uses milliseconds where Gin expects seconds. Add startup key validation, modern token/session lifecycle, rate limits, bounded/validated uploads, constant-time internal auth, cookie/CORS/header hardening, password/email/account controls, and remaining Appendix C closure. |
| 24 Performance/reliability | Partial; reachable defects confirmed | OpenAI bounds, extracted I/O, worker caps, harness/lint, Fable-approved bounded shutdown (`33f4dd4`), and public liveness/readiness probes | Hot-path indexes, analytics cache correctness, Redis recovery/timeouts, latency/query-plan evidence, and remaining launch-critical Appendix C ops items. Current quiz-answer follow-up still runs in an unprotected process goroutine, invalidates only premium caches, and can repopulate stale state; periodic renewal scheduling also launches a bare per-replica goroutine. |
| 25 AI cost optimization | Partial; every major strategy lever remains reachable | TTS language fix, image prompt fix, bounded provider jobs | Definition reuse still searches token only and ignores lookup errors; generation has no language-scoped uniqueness/coalescing. Definitions remain pinned to `gpt-4o` without an output-token cap or benchmarked model policy. Every new definition eagerly queues image and all selected example audio; audio object keys include definition ID, preventing global content dedup. Realtime sessions have no server-owned duration/token/day ceiling, while telemetry remains client-supplied and unbounded. Add benchmarked model selection, generation locks/constraints, lazy media, global content-addressed audio, image policy, trustworthy realtime quotas/telemetry, and before/after cost metrics. |

### Appendix C critical/high disposition (provisional)

`Pending/verify` is deliberately not completion. Each row needs a focused code/test proof before its tracker state can close.

| ID | Finding | Provisional disposition |
|---|---|---|
| C/H-01 | Broken wordlist update SQL and omitted pronunciation | Verified complete locally: owned full-contract updates and repository/service/HTTP regression evidence pass against real PostgreSQL |
| C/H-02 | Stripe status enum mismatch | Phase 4 removal; prove legacy ingress/workers are unreachable before deleting code |
| C/H-03 | Zero-value image-worker Leitner strategy | Verified complete locally: race-enabled prompt/strategy/not-found worker tests pass on 2026-08-06 |
| C/H-04 | Error-report regeneration erases learning history | Pending; destructive definition unlink remains reachable |
| C/H-05 | Word update wipes audio/notes | Verified complete locally: presence-aware owned updates preserve omitted fields and the mobile serializer strips server-owned media/processing fields |
| C/H-06 | Case-sensitive signup versus case-insensitive login | Pending; normalize/backfill/unique-index safely |
| C/H-07 | Shadowed/deferred transaction errors | Confirmed reachable. The definition worker now returns `tx.Commit` failures, but `WordService.SaveWord` and error-report `executeTransaction` still commit in deferred closures that can only log failure after the functions have selected successful return values. `SaveWord` also suppresses definition-reuse/tracking and job-enqueue failures. The 2026-08-06 full integration run repeatedly emitted `failed to rollback transaction: tx is closed`, confirming lifecycle cleanup remains noisy/inconsistent. |
| C/H-08 | Incorrect `errors.Is` not-found checks | Pending; three pointer comparisons remain |
| C/H-09 | Concurrent legacy webhook check-then-act | Accepted retirement design: Stripe boundary is transactional and RevenueCat becomes unreachable at cutover; prove flag/queue drain before removal |
| C/H-10 | RevenueCat event stored after side effects | Accepted retirement design; make ingress/worker unreachable and drain/discard queue in Phase 4 |
| C/H-11 | Deleted definition returns nil/nil to workers | Implementation corrected but proof incomplete: the service returns typed NotFound and both image/example-audio workers cancel that error; the race-enabled image-worker regression passes, but `API-DATA-4` must add equivalent focused example-audio coverage before closing this row. |
| C/H-12 | Definition worker swallows transactional errors | Confirmed reachable and partial. Required meaning-audio enqueue and final commit now propagate, but image-job enqueue, example-audio enqueue, Leitner inclusion, and completed-status failures are logged and continued. SQL failures may surface later through an aborted transaction, but typed/business failures can commit incomplete definitions and report the River job successful. |
| C/H-13 | RevenueCat lost-entitlement restore no-op | Phase 4 removal; native restore must pass Gate 3 first |
| C/H-14 | RevenueCat lifetime expiry nil dereference | Phase 4 removal; prove no reachable legacy restore before deletion |
| C/H-15 | Stripe `Items.Data[0]` panic | Phase 4 removal; prove no reachable legacy worker before deletion |
| C/H-16 | Provider switch creates double active subscription | Phase 4 removal plus zero-real-entitlement release audit; native schema constraints replace it |
| C/H-17 | Legacy subscription selects omit provider fields | Phase 4 removal; legacy reminder queues/jobs must be drained or discarded |
| C/H-18 | Stats cache write uses canceled errgroup context | Pending; current `Stats` shadows the parent context and writes after `g.Wait` |
| C/H-19 | Cache invalidation omits day suffix and free users | Pending; current keys still mismatch and invalidation remains premium-only. The 2026-08-06 full integration run repeatedly hit `redis: client is closed` across per-context tests, also proving singleton ownership/recovery needs explicit correction. |
| C/H-20 | Detached per-answer analytics goroutine can stale/panic | Partial; launch moved after commit, but goroutine remains unprotected and premium-only invalidation persists |
| C/H-21 | Empty JWT key permits authentication bypass | Pending launch blocker; middleware still reads the environment directly |
| C/H-22 | One-year nonrevocable plan-bearing JWT | Pending launch blocker; replace with short-lived access and rotating/revocable refresh contract |
| C/H-23 | No auth endpoint rate limits | Pending launch blocker |
| C/H-24 | Sentry/log PII leakage | Verified complete locally: race-enabled global logger, Sentry event/log, and call-site contract tests pass on 2026-08-06 |
| C/H-25 | Legacy checkout open redirect | Phase 4 removal; the entire route is flag-gated and must be absent at cutover |
| C/H-26 | Unbounded/unvalidated profile upload | Pending launch blocker |
| C/H-27 | Cross-tenant/global distractors | Verified complete locally: the full 2026-08-06 race-enabled PostgreSQL matrix passed exact cross-user/list/language/POS and option-count coverage |
| C/H-28 | No `word_definitions` indexes | Pending migration and query-plan milestone |
| C/H-29 | Corpus-wide `ORDER BY RANDOM()` | Partial; queries are now tenant/list/language/POS scoped, but random sort and hot-path plan still need measured remediation |
| C/H-30 | Unbounded OpenAI clients | Verified complete locally: full race-enabled `internal/openai` and worker-budget tests pass on 2026-08-06 |
| C/H-31 | Worker/database concurrency mismatch | Locally contained; production queue depth, job latency, DB capacity, and replica count remain an external measurement gate |
| C/H-32 | Inverted API shutdown | Verified complete locally in `33f4dd4`: Fable approved; race stress, vet, lint, format, and complete containerized API matrix pass |
| C/H-33 | No liveness/readiness endpoints | Verified complete locally: public dependency-free liveness and one-second database readiness contracts pass race-enabled tests and the full API matrix |
| C/H-34 | Recursive streak query/cache stampede | Pending measurement and redesign with user-level streak milestone |
| C/H-35 | Non-sargable learning-progress date query/index gap | Pending query/index milestone |

### Appendix C medium disposition (provisional)

Medium findings do not automatically block the IAP cutover. Security/privacy/data-corruption issues and any medium finding proven reachable on the launch path are promoted to release work; the remainder stay explicitly tracked as fix-as-touch or measured optimization.

| ID | Finding | Provisional disposition |
|---|---|---|
| M-01 | Incorrect `word_definitions` join attribution | Confirmed pending: candidate selection and answer ownership are correctly paired, but no-due diagnostics, box distribution, and skip-rescue/reset queries still join only on definition ID and can attribute another linked word |
| M-02 | UTC/database/user-local date mismatch | Confirmed pending: writes derive a Go UTC date while reads/streaks compare PostgreSQL `CURRENT_DATE`; neither uses the user's timezone |
| M-03 | Aggregate shared-definition deletion decision | Confirmed pending: one aggregate distinct-word count controls deletion for the whole definition set, so mixed exclusive/shared definitions are handled incorrectly; internal commit errors are also log-only |
| M-04 | Missing word/link uniqueness constraints | Partial: local migration 000077 now preflights and enforces canonical word-name identity per wordlist with reversible/locking evidence; `word_definitions(word_id,definition_id)` uniqueness and generation coalescing remain in `AI-GEN-1` |
| M-05 | Cross-user word reparenting | Verified complete locally: one PostgreSQL statement requires authenticated ownership of both source word and target wordlist before mutation |
| M-06 | Foreign word deletion returns 204 | Confirmed pending: service fetches the word without owner scope, owned DELETE returns row count zero, and the service returns `(0, nil)` |
| M-07 | Legacy grace-period anchor | Phase 4 removal; native lifecycle already owns grace semantics |
| M-08 | Legacy renewal provider/log mismatch | Phase 4 removal and queue drain |
| M-09 | Unknown Stripe price maps free | Phase 4 removal; legacy checkout must be unreachable |
| M-10 | Positional Expo push-ticket matching | Confirmed pending: `processTickets` still pairs `messages[i]` to `tickets[i]` and can deactivate the wrong token on partial/reordered responses |
| M-11 | Receipt loop lacks progress guarantee | Confirmed pending: failed `UpdateReceipt` calls only log, so a full response with persistent update failures refetches the same batch forever |
| M-12 | Invalid timezone aborts reminder cohort | Confirmed pending: both cohort queries evaluate user-controlled `pt.timezone` directly in one set-wide `AT TIME ZONE` query |
| M-13 | Nontransactional account deletion | Verified complete locally: the full 2026-08-06 race-enabled PostgreSQL matrix passed success and forced-final-delete rollback coverage |
| M-14 | Error-report limiter fails open/counts incorrectly | Partially stale but still pending: enforcement now counts real `error_reports`, so tracking-table increments are not authoritative; however hourly DB errors propagate into middleware `c.Next` and daily DB errors return nil, so both paths still fail open |
| M-15 | pgx-v3 no-row mismatch/dependency | Confirmed pending: `internal/repository/user.go` still imports both v3 and v5, compares v5 query errors to v3 `ErrNoRows`, and keeps v3 as a direct module dependency |
| M-16 | Learning progress mastered/count date range wrong | Partially stale but pending: `words_mastered` is still never written; the current strict `date > current_date - days` expression yields today plus the preceding `days-1` dates, so verify the intended window contract before changing it |
| M-17 | User token in OpenAI system prompt | Pending prompt-boundary/security milestone |
| M-18 | Legacy webhook bypass/authorization defects | RevenueCat compare/config fixed; remove Stripe bypass and all legacy ingress in Phase 4 |
| M-19 | Login timing side channel | Pending authentication hardening |
| M-20 | Weak/inconsistent password and production bcrypt policy | Pending authentication hardening |
| M-21 | Deprecated JWT library/unverified environment | Pending token-lifecycle launch blocker |
| M-22 | CORS fail-open, missing headers, middleware abort | Confirmed pending with one narrowed subfinding: production allowlisting exists, but every non-`production` ENV still reflects credentialed origins, no security-header middleware exists, and the invalid-user-type subscription-limit branch returns JSON without `c.Abort()` |
| M-23 | Untrusted/unbounded realtime telemetry | Pending realtime quota/telemetry milestone |
| M-24 | Cookie age units/domain | Pending authentication hardening |
| M-25 | Nonconstant-time static auth | Pending internal-route security hardening |
| M-26 | Expensive no-due diagnostic query | Pending measured quiz-path optimization |
| M-27 | Oversized quiz candidate materialization | Pending measured query-plan optimization |
| M-28 | Unbounded/list/filter response queries | Pending API boundary and pagination milestone |
| M-29 | Per-upload MinIO construction/buffering | Pending upload/security/performance milestone |
| M-30 | Timeout middleware did not cancel work | Verified complete locally: all race-enabled normal, timeout, context-propagation, cancellation, double-response, duration, and concurrency tests pass on 2026-08-06 |
| M-31 | pgBouncer transaction-pool incompatibility | Pending deployment/database configuration proof |
| M-32 | Unsafe migration timeout/index policy | Pending migration-runner hardening; production application owner-gated |
| M-33 | Redis failure permanently memoized/no I/O timeouts | Pending cache/Redis recovery milestone |
| M-34 | River retry/timeout/uniqueness defaults | Partial; paid/provider workers gained bounds and selected uniqueness, but audit every registered job |
| M-35 | Bare per-replica renewal goroutine | Pending worker scheduling/idempotency milestone or Phase 4 legacy removal proof |
| M-36 | Per-job `runtime.ReadMemStats` | Pending measured worker-observability cleanup |
| M-37 | Container/root/image/health/default-secret debt | Pending deployment-hardening milestone; distinguish local-compose defaults from production images |

### Dependency sequence

This sequence distinguishes roadmap completion from the narrower IAP cutover gate. Next-wave items remain executable and tracked but cannot delay Phase 4 unless they expose a correctness, security, compliance, or store blocker.

1. **Production shutdown milestone complete.** `API-SHUT-1` landed locally as `33f4dd4` after Fable approval, deterministic deadline/force-close/in-flight-drain coverage, race stress, static gates, and the complete containerized API matrix.
2. **Review this consolidated catalog.** Run a fresh Fable-first Claude planning review over the exact strategy/Appendix references, acceptance evidence, dependencies, and release classifications before coding newly discovered items; reconcile every material finding in this file.
3. **Correct reachable data contracts.** Wordlist update/pronunciation plus partial word updates/target ownership are complete locally; next repair not-found mapping, definition/error-report transaction semantics, email normalization/uniqueness, and other confirmed C/H data-integrity defects in separate test-first items.
4. **Harden authentication before adding social identity.** Fail startup on invalid key/config, replace the deprecated one-year claim-heavy token with short-lived access plus rotating/revocable refresh state, define logout/password-reset invalidation, then add auth rate limits, timing protections, password policy, cookie/CORS/header/static-auth controls.
5. **Bound uploads and request surfaces.** Add global/per-route body limits, validated image decoding/type/size/dimensions and server-owned object suffixes, then close unbounded list/telemetry inputs and ownership checks.
6. **Make deploy/runtime health observable.** `/healthz` and DB-backed `/readyz` are complete locally; next repair analytics caching/Redis recovery, add hot-path indexes and query-plan evidence, then address detached goroutines and remaining launch-path scheduling/idempotency risks.
7. **Prevent activation features from multiplying AI cost.** Add language-correct uniqueness and generation coalescing, benchmark/model-output limits, and server-owned Realtime quotas before starter packs, bulk import, or wider free acquisition can increase provider spend. Lazy/global-deduplicated media follows before bulk/OCR launch but is not an IAP-cutover prerequisite.
8. **Build the activation/conversion chain.** Social sign-in (after auth hardening) → global point-of-intent native paywall → progressive limits/gating → starter personalization/content → finite quiz completion → user-level streak/daily goal → due/practice dashboard → bottom tabs and free Progress preview.
9. **Layer engagement surfaces on stable contracts.** Quiz/streak haptics and motion → expanded reminders/preferences/channels → share cards/widgets → dismissal/win-back mechanics. Store-configured offers and physical-device/widget/store proofs remain owner-gated.
10. **Run fix-as-touch continuously and close globally.** Every UI item includes i18n, accessibility, reduced motion, responsive states, analytics, and relevant Maestro evidence; finish with locale/orphan/default-key automation, design-system docs, full mobile/API/web/static suites, full Android Maestro, then owner-controlled iOS/store/production gates.
11. **Perform Phase 4 only after Gate 3.** Re-run the production zero-entitlement audit, drain/discard legacy queues, disable the complete legacy surface, ship/prove native entitlement lifecycle, remove Stripe/RevenueCat code/schema/env/dependencies, prove credential-free startup/web disabled behavior, monitor, then remove rollback scaffolding.

### Milestone catalog

Classification meanings:

- `CUTOVER`: explicit correctness, security, data, store, or release condition that may block the IAP-only release.
- `REVAMP`: required strategy work that remains autonomous/local where possible but does not by itself block provider removal.
- `NEXT`: strategy next-wave work; tracked, dependency-ordered, and nonblocking by default.
- `OWNER`: external evidence or mutation that Codex must not fabricate or perform without authorization.

Cutover classification is intentionally narrow:

- `API-DATA-1`–`API-DATA-6` and `API-TX-1` protect reachable writes, ownership, learning history, and response truthfulness; they are correctness/data gates, not visual-revamp prerequisites.
- `AUTH-1`–`AUTH-3`, `HTTP-SEC-1`, `UPLOAD-1`, `API-BOUND-1`, and `API-RATE-1` close reachable authentication, abuse, cross-owner, and unbounded-input paths; they are security gates.
- `API-SHUT-1`, `OPS-HEALTH-1`, `ASYNC-1`, `OPS-DB-1`, `OPS-MIG-1`, and `OPS-DEPLOY-1` cover process survival, deploy readiness, database compatibility/migration safety, and production runtime hardening; they are release gates only while the audited defects remain reachable in the release artifact.
- `QA-COVERAGE-1` is a release gate because the repository instructions and existing `make coverage-threshold` command already require 70% unit and 80% integration coverage; this classification does not authorize changing those thresholds.
- `PHASE3-OWNER` and `PHASE4-CUTOVER` are the actual store/live cutover gates.
- All `REVAMP` and `NEXT` items remain required by the full strategy but cannot delay provider removal by default. Promotion to `CUTOVER` requires a dated evidence note proving a reachable security, correctness, data, store, or release defect; product polish, speculative scale, conversion impact, or optimization value alone is insufficient.

| ID | Goal | Class | Depends on |
|---|---|---|---|
| MAP-1 | Reconcile all 25 priorities and 72 Appendix C findings into the execution tracker with Claude agreement | REVAMP; complete locally on 2026-08-06 | API-SHUT-1 |
| API-SHUT-1 | Gracefully drain the production HTTP server on SIGINT/SIGTERM with a bounded timeout | CUTOVER; complete locally in `33f4dd4` | None |
| API-DATA-1 | Restore the full owned wordlist update contract, including pronunciation | CUTOVER; complete locally on 2026-08-06 | MAP-1 |
| API-DATA-2 | Make word partial updates preserve omitted fields and reject cross-owner reparenting | CUTOVER; complete locally on 2026-08-06 | API-DATA-1 |
| API-DATA-3 | Normalize email identity and enforce case-insensitive uniqueness without locking out existing rows | CUTOVER | MAP-1, production preflight before live migration |
| API-DATA-4 | Return typed not-found/ownership errors consistently across word and wordlist routes | CUTOVER | API-DATA-2 |
| API-DATA-5 | Preserve learning history during error-report regeneration and make every substep atomic/retryable | CUTOVER | API-DATA-2 |
| API-DATA-6 | Correct word-to-definition attribution in diagnostics, box distributions, and skip/rescue paths | CUTOVER | API-DATA-2 |
| API-TX-1 | Eliminate remaining deferred/shadowed commit failures on reachable write paths | CUTOVER | API-DATA-5 |
| AUTH-1 | Fail startup on invalid auth configuration and replace deprecated claim-heavy year-long JWT access | CUTOVER | MAP-1 |
| AUTH-2 | Add rotating/revocable refresh state and invalidate sessions on logout/password reset/change | CUTOVER | AUTH-1, API-DATA-3 |
| AUTH-3 | Rate-limit signup/login/reset flows and close login timing/password/bcrypt policy gaps | CUTOVER | AUTH-1 |
| HTTP-SEC-1 | Add secure cookies, fail-closed CORS, security headers, correct abort behavior, and constant-time static auth | CUTOVER | AUTH-1 |
| QA-LOG-1 | Redact authorization/cookie headers and fixture identifiers from verbose integration/CI HTTP output without hiding assertion context | REVAMP | HTTP-SEC-1 |
| QA-COVERAGE-1 | Repair unit-test discovery, then raise real aggregate API coverage from 23.7% unit/39.8% integration to the unchanged 70% unit and 80% integration thresholds without exclusions or denominator tricks | CUTOVER | MAP-1; may proceed package-by-package beside disjoint milestones |
| UPLOAD-1 | Bound and validate profile uploads with server-owned object naming and safe storage behavior | CUTOVER | HTTP-SEC-1 |
| API-BOUND-1 | Bound list/batch/telemetry payloads and enforce ownership without exposing password material | CUTOVER | HTTP-SEC-1 |
| API-RATE-1 | Make error-report and other stateful quota checks fail closed with accurate counters | CUTOVER | API-BOUND-1 |
| OPS-HEALTH-1 | Expose unauthenticated liveness and bounded database readiness endpoints | CUTOVER; complete locally on 2026-08-06 | API-SHUT-1 |
| CACHE-1 | Make analytics cache writes/invalidation correct for all plans and recover Redis after transient boot failure | REVAMP | MAP-1 |
| DB-PERF-1 | Add reversible hot-path indexes and prove query-plan use locally before owner-gated production application | REVAMP | API-DATA-2 |
| ASYNC-1 | Replace unsafe detached analytics/renewal goroutines with panic-safe durable or bounded work | CUTOVER | CACHE-1 |
| ASYNC-2 | Audit every registered River job for bounded attempts, timeout, uniqueness, cancellation, and paid-side-effect idempotency | REVAMP | ASYNC-1 |
| OPS-CAP-1 | Measure worker queue/database capacity and encode safe per-queue/pool/replica operating limits | REVAMP/OWNER | DB-PERF-1, ASYNC-2 |
| OPS-DB-1 | Prove or configure pgx compatibility with the deployed pgBouncer pooling mode | CUTOVER/OWNER | OPS-HEALTH-1 |
| OPS-MIG-1 | Add migration lock/statement bounds and a safe concurrent-index deployment procedure | CUTOVER/OWNER | DB-PERF-1 |
| OPS-OBS-1 | Remove per-job stop-the-world memory sampling or replace it with measured low-overhead telemetry | NEXT | ASYNC-2 |
| OPS-DEPLOY-1 | Harden production images/runtime health, non-root execution, version pinning, limits, and secret defaults | CUTOVER/OWNER | OPS-HEALTH-1 |
| PUSH-1 | Correct push-ticket/receipt/timezone progress guarantees before expanding notifications | REVAMP | MAP-1 |
| ANALYTICS-1 | Define timezone-correct daily windows, mastered-word writes, and inclusive range semantics | REVAMP | API-DATA-6, DB-PERF-1 |
| AI-GEN-1 | Make definition identity language-correct and coalesce concurrent generation | REVAMP | API-DATA-2, DB-PERF-1 |
| AI-MODEL-1 | Benchmark definition quality and enforce a right-sized model/output-token policy | REVAMP | AI-GEN-1 |
| AI-MEDIA-1 | Lazily generate and globally deduplicate eligible audio/images with idempotent retries | NEXT | AI-GEN-1 |
| REALTIME-1 | Enforce server-owned Realtime session/token/day quotas and trustworthy bounded telemetry | REVAMP | AUTH-2, API-BOUND-1 |
| UX-AUTH-1 | Add Apple/Google sign-in and safe existing-account linking on the hardened session model | REVAMP | AUTH-2, AUTH-3 |
| UX-ANALYTICS-1 | Complete trigger/dismissal/permission/flashcard/voice events and a typed feature-flag experiment boundary | REVAMP | MAP-1 |
| UX-PAYWALL-1 | Open the native paywall in context from every upgrade trigger with real store terms | REVAMP | UX-ANALYTICS-1, Phase 2 IAP contract |
| UX-LIMITS-1 | Show honest server-owned usage/locks and preserve user context when a free limit is reached | REVAMP | UX-PAYWALL-1, API-DATA-2 |
| UX-DEGRADE-1 | Replace silent free-tier null/stale states with explicit retry, limitation, and contextual-upgrade states | REVAMP | UX-LIMITS-1, CACHE-1 |
| UX-STARTER-1 | Shorten onboarding and add a pre-auth interactive demo plus language/level/goal personalization | REVAMP | UX-AUTH-1, AI-GEN-1 |
| UX-STARTER-2 | Add curated starter packs with bounded idempotent claim/import semantics | REVAMP | UX-STARTER-1, AI-MODEL-1 |
| UX-QUIZ-1 | End finite quiz sessions with an accessible summary and stable next actions | REVAMP | UX-ANALYTICS-1 |
| UX-STREAK-1 | Implement a timezone-correct user-level streak and daily-goal contract | REVAMP | API-DATA-2, DB-PERF-1 |
| UX-STREAK-2 | Add freeze/repair policy and 3/7/30/100 milestone states without pay-to-preserve coercion | NEXT | UX-STREAK-1 |
| UX-DUE-1 | Render due counts, a primary practice CTA, and app-badge lifecycle | REVAMP | Existing due API, UX-ANALYTICS-1 |
| UX-TABS-1 | Add Home/Practice/Progress/Profile tabs using the approved primitives and stable deep links | REVAMP | UX-DUE-1 |
| UX-PROGRESS-1 | Give free users current-week progress and show honest locked historical previews | REVAMP | UX-TABS-1, CACHE-1, UX-STREAK-1 |
| UX-MOTION-1 | Add reduced-motion-safe answer/session/streak feedback with haptics/sound as optional reinforcement | REVAMP | UX-QUIZ-1, UX-STREAK-1 |
| UX-BULK-1 | Add bounded paste-list bulk entry within limits | NEXT | UX-LIMITS-1, AI-GEN-1 |
| UX-BULK-2 | Add CSV/Anki import and separately decide whether privacy-safe premium OCR is justified | NEXT | UX-BULK-1, AI-MEDIA-1 |
| UX-NOTIFY-1 | Add user reminder preferences, Android channels, and goal-framed permission timing | NEXT | UX-STREAK-1, PUSH-1 |
| UX-NOTIFY-2 | Add streak-risk, enrichment-ready, recap, and win-back notifications under explicit frequency rules | NEXT | UX-NOTIFY-1 |
| UX-SHARE-1 | Build privacy-safe streak/milestone share cards | NEXT | UX-STREAK-2, UX-QUIZ-1 |
| UX-WIDGET-1 | Spike and then implement iOS/Android due/streak widgets with practice deep links | NEXT/OWNER | UX-DUE-1, UX-STREAK-1, native-device credentials |
| UX-OFFER-1 | Add measured Nth-dismissal/win-back/code redemption only from real store-configured offers | NEXT/OWNER | UX-PAYWALL-1, production funnel window, store offer configuration |
| QUALITY-1 | Enforce locale parity, no hidden defaults/orphans, and accurate design-system documentation | REVAMP | All touched UI milestones, then final closure |
| QUALITY-2 | Complete screen-level accessibility/dynamic-type/dark-mode and reduced-motion audits | REVAMP/OWNER | All touched UI milestones, physical-device final gate |
| QUALITY-CODE-1 | Measure and reduce touched-screen render/style debt, decompose unstable god files, and enforce regression budgets | REVAMP | Core activation UI milestones |
| PHASE3-OWNER | Execute Apple/Google sandbox, production plumbing, metadata, device, catalog, and smoke matrix | OWNER/CUTOVER | Existing Phase 2 IAP contract and local Phase 3 preparation; `QA-COVERAGE-1` before Gate 3 closes |
| PHASE4-CUTOVER | Disable/drain legacy providers, release/prove native IAP, delete Stripe/RevenueCat, validate and monitor | OWNER/CUTOVER | `PHASE3-OWNER`, `API-DATA-1`, `API-DATA-2`, `API-DATA-3`, `API-DATA-4`, `API-DATA-5`, `API-DATA-6`, `API-TX-1`, `AUTH-1`, `AUTH-2`, `AUTH-3`, `HTTP-SEC-1`, `QA-COVERAGE-1`, `UPLOAD-1`, `API-BOUND-1`, `API-RATE-1`, `API-SHUT-1`, `OPS-HEALTH-1`, `ASYNC-1`, `OPS-DB-1`, `OPS-MIG-1`, `OPS-DEPLOY-1` |

### Traceability and acceptance allocation

The strategy headings at `MOBILE_APP_REVAMP_STRATEGY.md:42-449` and Appendix C rows at `:483-568` are authoritative. A priority or finding remains open until every allocated milestone is complete; completing one milestone never closes the whole source row by implication.

| Strategy priority | Owning milestones |
|---|---|
| 1 | Existing first-launch item; `AUTH-1`–`AUTH-3`, `UX-AUTH-1` |
| 2 | `UX-PAYWALL-1`, `UX-ANALYTICS-1`, `UX-OFFER-1` |
| 3 | `UX-ANALYTICS-1`, Phase 0.5 production measurement owner gate |
| 4 | `UX-STARTER-1`, `UX-STARTER-2` |
| 5 | `UX-QUIZ-1`, `UX-MOTION-1`, `UX-PAYWALL-1` |
| 6 | `UX-STREAK-1`, `UX-STREAK-2`, `ANALYTICS-1` |
| 7 | `UX-DUE-1`, `UX-NOTIFY-1` |
| 8 | Existing primitive item; `UX-TABS-1`, `QUALITY-1`, `QUALITY-2` |
| 9 | `UX-MOTION-1`, `QUALITY-2`, physical-device owner gate |
| 10 | `UX-LIMITS-1`, `UX-PAYWALL-1`, `UX-DEGRADE-1` |
| 11 | Existing native-IAP implementation; `PHASE3-OWNER`, `PHASE4-CUTOVER` |
| 12 | `UX-OFFER-1` |
| 13 | `UX-BULK-1`, `UX-BULK-2` |
| 14 | `PUSH-1`, `UX-NOTIFY-1`, `UX-NOTIFY-2` |
| 15 | `UX-SHARE-1`, `UX-WIDGET-1` |
| 16 | `UX-PROGRESS-1`, `CACHE-1`, `ANALYTICS-1` |
| 17 | `UX-DEGRADE-1`, `CACHE-1` |
| 18 | `QUALITY-2` plus every UI milestone's prototype/implementation gate |
| 19 | `QUALITY-CODE-1`, `QA-COVERAGE-1`, `DB-PERF-1`, final cross-project validation |
| 20 | `QUALITY-1` |
| 21 | Existing IAP Phases 1–2; `PHASE3-OWNER`, `PHASE4-CUTOVER` |
| 22 | `API-DATA-1`–`API-DATA-6`, `API-TX-1`, `API-RATE-1`, plus verified-complete defect rows |
| 23 | `AUTH-1`–`AUTH-3`, `HTTP-SEC-1`, `UPLOAD-1`, `API-BOUND-1`, `REALTIME-1`, `PHASE4-CUTOVER` |
| 24 | `API-SHUT-1`, `OPS-HEALTH-1`, `CACHE-1`, `DB-PERF-1`, `ASYNC-1`, `ASYNC-2`, `OPS-CAP-1`, `OPS-DB-1`, `OPS-MIG-1`, `OPS-OBS-1`, `OPS-DEPLOY-1` |
| 25 | Existing OpenAI/TTS corrections; `AI-GEN-1`, `AI-MODEL-1`, `AI-MEDIA-1`, `REALTIME-1`, `OPS-OBS-1` |

| Appendix C findings | Owning milestone or terminal proof |
|---|---|
| C/H-01 | `API-DATA-1` |
| C/H-02 | `PHASE4-CUTOVER` |
| C/H-03 | Verified-complete image-worker evidence row |
| C/H-04 | `API-DATA-5` |
| C/H-05 | `API-DATA-2` |
| C/H-06 | `API-DATA-3` |
| C/H-07 | `API-TX-1` |
| C/H-08 | `API-DATA-4` |
| C/H-09–10 | `PHASE4-CUTOVER` |
| C/H-11 | `API-DATA-4` (image path proven; example-audio regression still required) |
| C/H-12 | `API-DATA-5`, `API-TX-1` |
| C/H-13–17 | `PHASE4-CUTOVER` |
| C/H-18–19 | `CACHE-1` |
| C/H-20 | `ASYNC-1` |
| C/H-21 | `AUTH-1` |
| C/H-22 | `AUTH-1`, `AUTH-2` |
| C/H-23 | `AUTH-3` |
| C/H-24 | Verified-complete privacy evidence row |
| C/H-25 | `PHASE4-CUTOVER` |
| C/H-26 | `UPLOAD-1` |
| C/H-27 | Verified-complete distractor evidence row |
| C/H-28–29 | `DB-PERF-1` |
| C/H-30 | Verified-complete OpenAI bounds evidence row |
| C/H-31 | Existing worker containment; `OPS-CAP-1` production measurement gate |
| C/H-32 | `API-SHUT-1` |
| C/H-33 | `OPS-HEALTH-1` |
| C/H-34 | `UX-STREAK-1`, `CACHE-1`, `DB-PERF-1` |
| C/H-35 | `DB-PERF-1` |
| M-01 | `API-DATA-6` |
| M-02 | `ANALYTICS-1`, `UX-STREAK-1` |
| M-03 | `API-DATA-5`, `API-TX-1` |
| M-04 | `API-DATA-2`, `AI-GEN-1` |
| M-05 | `API-DATA-2` |
| M-06 | `API-DATA-4` |
| M-07–09 | `PHASE4-CUTOVER` |
| M-10–12 | `PUSH-1` |
| M-13 | Verified-complete atomic account-deletion evidence row |
| M-14 | `API-RATE-1` |
| M-15 | `AUTH-1` |
| M-16 | `ANALYTICS-1` |
| M-17 | `AI-GEN-1` |
| M-18 | `PHASE4-CUTOVER` |
| M-19–21 | `AUTH-1`–`AUTH-3` |
| M-22 | `HTTP-SEC-1` |
| M-23 | `REALTIME-1`, `API-BOUND-1` |
| M-24 | `AUTH-2`, `HTTP-SEC-1` |
| M-25 | `HTTP-SEC-1` |
| M-26–27 | `DB-PERF-1` |
| M-28 | `API-BOUND-1` |
| M-29 | `UPLOAD-1` |
| M-30 | Verified-complete timeout middleware evidence row |
| M-31 | `OPS-DB-1` |
| M-32 | `OPS-MIG-1` |
| M-33 | `CACHE-1` |
| M-34 | `ASYNC-2` |
| M-35 | `PHASE4-CUTOVER`; use `ASYNC-1` only if legacy scheduling remains reachable |
| M-36 | `OPS-OBS-1` |
| M-37 | `OPS-DEPLOY-1` |

Milestone acceptance evidence is cumulative: a focused regression must fail before implementation and pass after it; all affected package and cross-contract suites must pass; static checks and `git diff --check` must pass; Claude must return substantive approval after reconciliation; UI items additionally require the approved prototype, accessibility/state matrix, scoped Maestro evidence, and motion/device gates where applicable; migrations require duplicate/preflight, up/down, locking and rollback evidence without applying production state; owner-class items remain open until live evidence is attached. Every completion note names the source priority/finding IDs it closes and any residual owner gate.

- `QA-COVERAGE-1`: `make test-unit` runs every unit test package under `./internal/...` (including the 13 existing in-package tests and `internal/tests/unit`) and a clean `make test-integration` runs every intended integration package; both commands produce nonempty profiles over `./internal/...`. `go tool cover -func` then reports at least 70% unit and 80% integration statements, `make coverage-threshold` exits zero with both unchanged thresholds, and added tests assert behavior rather than merely executing lines. The exact containerized `make test` remains green and produces its own nonempty current profile; unit and integration coverage cannot substitute for each other.

### Parallel lane allocation

The catalog supports hybrid parallelism, not unrestricted concurrency. Before opening worktrees, the integration owner records the exact milestone IDs, branches, allowed paths, dependency SHA, and merge order in the execution plan. The following are maximum scopes; a milestone narrows them further.

| Lane | Milestones | Primary write scope | Collision and merge rule |
|---|---|---|---|
| Integration/tracker | `MAP-1`, shared contracts, final validation | `docs/MOBILE_APP_REVAMP_EXECUTION_PLAN.md`, cross-package contract docs, migration-number allocation | Sole owner of the tracker and next migration number; contract lands before consumer lanes rebase |
| API data | `API-DATA-1`–`API-DATA-6`, `API-TX-1`, `ANALYTICS-1` | wordlist/word/definition/error-report/analytics repository-service-handler code and focused tests | Serializes with auth on `repository/user.go`, with AI on definition/word services, and with UX when an API shape changes |
| Auth/security | `AUTH-1`–`AUTH-3`, `HTTP-SEC-1`, `UPLOAD-1`, `API-BOUND-1`, `API-RATE-1` | user/session/middleware/upload/internal-auth code, security migrations, focused tests | One auth milestone at a time; contract first before `UX-AUTH-1` or `REALTIME-1`; no concurrent edits to setup/middleware/user files |
| Runtime/operations | `API-SHUT-1`, the six `OPS` milestones, `CACHE-1`, `DB-PERF-1`, `ASYNC-1`, `ASYNC-2` | `cmd/`, common database/Redis/runtime code, River configuration, Docker/Compose, ops tests/docs | Can parallelize only when files and migrations are disjoint; health/shutdown integrate before deploy-image work; capacity remains measurement-gated |
| Push | `PUSH-1`, `UX-NOTIFY-*` backend tranche | push repositories/services/workers and focused tests | Backend correctness lands before notification UX; respects the global two-per-seven-days cap |
| AI | `AI-GEN-1`, `AI-MODEL-1`, `AI-MEDIA-1`, `REALTIME-1` provider tranche | OpenAI clients, enrichment workers/services, cost/quota tests | Serializes with API data on word/definition services and with auth on Realtime quota identity; no provider experiment before benchmark contract |
| Mobile activation | the 21 `UX` milestones except store-owner execution | One approved mockup directory plus the item's mobile screens/components/hooks/tests/i18n keys | One UI-bearing milestone at a time because routing, shared primitives, and eight locale files collide; API contracts must already be merged |
| Quality evidence | `QA-LOG-1`, `QA-COVERAGE-1`, `QUALITY-1`, `QUALITY-2`, `QUALITY-CODE-1` | Disjoint test packages, validation scripts, accessibility/locale tooling, documentation | Test-only coverage tranches may parallelize after the owning behavior SHA; they cannot rewrite production behavior merely to inflate coverage |
| Store/cutover | `PHASE3-OWNER`, `PHASE4-CUTOVER` | Owner-run store/production operations followed by the isolated provider-removal diff | Strictly sequential; no provider deletion before Gate 3 evidence and no external mutation by an autonomous lane |

Combined validation is mandatory after every merge. A lane's isolated green state is provisional until its dependency SHA is rebased, conflicts are reconciled by the integration owner, the roadmap completion note is updated, and the aggregate relevant suites pass on the execution branch.

### Data correctness milestone acceptance boundaries

The remaining source still proves each pending milestone is reachable: zero-row deletes can report success; email creation remains case-sensitive while login normalizes; error-report regeneration deletes associations that cascade learning history; definition cleanup decides shared ownership from one aggregate count; several transactions only log deferred commit failures; and diagnostic/box/skip queries still join a definition without the matching word.

- `API-DATA-1`: complete locally on 2026-08-06. Replaced the malformed update with one owned PostgreSQL statement covering name, description, language, pronunciation, and `updated_at`. Explicit pronunciation must be compatible with the submitted language; omission atomically preserves the pre-update value when language is unchanged and chooses the new language default when it changes. HTTP and service boundaries reject nonpositive IDs, zero owned rows remain a typed not-found, and the mutation predicate retains both owner and wordlist IDs. Fable verified PostgreSQL pre-update CASE/EvalPlanQual semantics, pgx NULL typing, validation, ownership, callers, and error/status mapping before returning `APPROVED`. A real-PostgreSQL race-enabled integration contract exercises repository, service, and HTTP layers for explicit changes, omission, language-default transition, invalid IDs, incompatible systems, cross-owner attempts, and unchanged-state guarantees; it passes three consecutive runs. Exact containerized `make test`, full lint/format checks, focused vet, plan validation, and diff checks pass. No migration or external mutation was required.
- `API-DATA-2`: complete locally on 2026-08-06. Public updates use pointer-shaped user-editable fields, while one owned PostgreSQL statement preserves omitted name, notes, learned, audio, and pronunciation values and treats the route wordlist as a target that must share the authenticated owner with the source word. Explicit notes clearing and `learned=false` remain distinguishable from omission; names retain the create path's trim/lowercase/15-rune policy; the mobile serializer emits only name, notes, learned, and matching wordlist identity, so full server models cannot forge media or processing fields. Duplicate create/update races map to a safe business response through reversible migration 000077's canonical `(wordlist_id, LOWER(BTRIM(name)))` unique index. Fable first identified raw-name identity and missing locking/dirty-recovery evidence, then approved after the preflight/index/runbook and legacy-case regression were reconciled. The old implementation failed the new end-to-end contract; the final real-PostgreSQL race test passes three consecutive runs and covers learned-only, explicit notes clearing, normalized/legacy rename, authorized move, cross-owner source/target, stale ID, forged fields, duplicate create, concurrent collision, and concurrent foreign quota non-inflation. A disposable database proves canonical mixed-case duplicate rejection, dirty-77 recovery, up/index-validity/23505/down behavior, and cleanup. Exact containerized `make test`, API `make test-unit`, all 34 mobile suites/251 tests, API/mobile static checks, plan validation, and diff checks pass. Production preflight/application remains owner-gated under `OPS-MIG-1`; `word_definitions` link uniqueness and generation coalescing remain explicitly allocated to `AI-GEN-1`.
- `API-DATA-3`: choose one canonical email normalization function and apply it before every create, login, lookup, reset, and identity-link operation; enforce database-level case-insensitive uniqueness with an indexed canonical expression or column. Before the owner-gated live migration, run a read-only collision report and define deterministic handling for any duplicates rather than letting index creation lock users out. Tests cover casing, Unicode/whitespace policy, concurrent signup, login/reset consistency, query-plan index use, reversible local migration, and collision preflight.
- `API-DATA-4`: standardize `common.NotFoundError` on one value/pointer contract and detect it with `errors.As`, never pointer-shaped `errors.Is` comparisons. Every word/wordlist read, update, and delete validates route IDs, checks ownership, distinguishes zero affected rows from success, returns the same non-enumerating 404 shape, and does not panic or report expected misses to Sentry. Cover malformed IDs, absent IDs, cross-owner access, deleted definitions in both media workers, and empty authorized collections.
- `API-DATA-5`: regeneration may detach or supersede only the reported word-definition relationship; it must preserve `leitner_system_tracking`, `quiz_performance`, prior review history, and unrelated/shared definitions. Determine shared/exclusive status per definition, not from one aggregate count, and perform snapshot, cooldown, skip state, association transition, durable job insertion, and report persistence atomically. Commit failure, worker insertion failure, crash/retry, shared-definition, unrelated meaning/example, processing-failed, and deleted-definition tests prove no history loss, orphaning, duplicate generation, or false success.
- `API-DATA-6`: every query that attributes a tracking row to a definition must join both `wd.definition_id = lst.definition_id` and `wd.word_id = lst.word_id`, plus authenticated user/wordlist scope where applicable. Correct box distribution, unlearned counts, diagnostics, and skip/rescue decisions together; use adversarial fixtures where two words share or cross-associate definitions so inflated counts cannot pass accidentally. Preserve the already-correct distractor boundary and include query-plan evidence after `DB-PERF-1` indexes land.
- `API-TX-1`: replace reachable deferred commit/rollback patterns that depend on shadowable outer `err` with an explicit managed-transaction helper or explicit commit path whose commit error is returned. Never continue after a failed required substep, never report a job/request successful after a lost commit, and treat `pgx.ErrTxClosed` rollback after commit as benign while surfacing real rollback failures observably. The audit covers definition save/delete, word save, error reporting, definition generation, Leitner writes, and every other `Begin`; deferred-constraint tests prove commit failures propagate and retry-safe inserts do not duplicate whole definition sets.
- `DB-PERF-1`: capture representative row counts and `EXPLAIN (ANALYZE, BUFFERS)` baselines for definition lookup, scoped distractors, no-due diagnostics, progress/date windows, streak, and worker scans before changing them. Add only evidence-backed reversible indexes or bounded query rewrites, prove the intended index and tenant predicates are used without regressing writes, and rerun adversarial correctness fixtures plus latency/query-count budgets. Concurrent production index application belongs to `OPS-MIG-1` and remains owner-gated; a fast empty local database is not acceptance evidence.

### Analytics, push, and AI acceptance boundaries

- `ANALYTICS-1`: define one user-local learning-day function from an IANA timezone with an explicit UTC fallback, pass its half-open UTC bounds into writes and reads, and remove mixed Go-UTC/PostgreSQL-session `CURRENT_DATE` semantics. Write `words_mastered` from a documented mastery transition without double-counting, make an N-day request return exactly N user-local dates including today, and use sargable timestamp predicates. Tests cover UTC offsets, DST transitions, invalid/missing zones, midnight races, 1/7/30-day cardinality, repeated mastery, unlearning policy, and aggregate/backfill compatibility.
- `PUSH-1`: bind each Expo ticket to its originating token using a contract that remains correct for partial/malformed responses; persist ticket/receipt state before counting a notification toward the rolling two-per-user/seven-day cap; and make receipt batches advance, defer, or terminally classify every fetched row so persistent update failure cannot loop forever. Invalid stored timezone values are isolated per user with an observable fallback rather than aborting the whole candidate query. Tests cover partial and out-of-order ticket data, duplicate tokens, transient send/receipt/storage failures, device-not-registered deactivation, no-progress batches, cancellation, invalid timezones, two-replica concurrency, and the combined daily-plus-due global cap.
- `AI-GEN-1`: scope definition reuse and identity by normalized token plus language and any meaningfully distinct generation contract; never reuse an English definition for the same token in another wordlist language. Enforce database uniqueness for word-definition links and an intentional word identity policy, then coalesce concurrent cache misses with a durable claim/idempotency key so one provider generation feeds all eligible waiters. Prove crash/retry, concurrent adds, language separation, stale claims, provider failure, and account deletion without duplicate paid calls or cross-language content.
- `AI-MODEL-1`: create a versioned, blinded benchmark corpus across every supported language and representative parts of speech, with schema-validity, factual/linguistic quality, latency, retry, and estimated cost measures. Compare candidate models/configurations under the same prompt and output contract, record human/Claude rubric review without treating either as sole truth, and select the cheapest option that clears predeclared quality/reliability thresholds. Model name, temperature, output-token ceiling, prompt version, rollback switch, and production experiment bounds are explicit; no model downgrade ships from cost intuition alone.
- `AI-MEDIA-1`: define eligibility and on-demand triggers before generating image, word audio, example audio, or meaning audio; globally reuse only when token/language/voice/content hash and policy match. Store a durable generation record with state, provider request/idempotency identity where available, object checksum, ownership/reference count, retry classification, and cleanup semantics so crash-after-provider/upload/database boundaries cannot duplicate spend or delete shared media. Tests cover simultaneous requests, stale processing claims, provider timeout, upload success plus commit failure, object collision, unsupported language/content, deletion, and manual regeneration.
- `REALTIME-1`: authorize the requested wordlist against the authenticated user before issuing a provider token; enforce server-owned per-session, concurrent-session, daily duration/token/cost ceilings from durable usage rather than client-submitted totals. Bound telemetry body, turns, strings, numeric ranges, clock skew, and idempotency, associate events with a server-issued session ID, and reject replay/cross-user updates. Tests cover token issuance races, disconnect without final telemetry, reconnect, forged totals, duplicate delivery, stale access, downgrade/revocation, quota boundary, and provider failure with fail-closed premium spend.

### Product and mobile acceptance boundaries

Every UI-bearing item follows the committed HTML/React prototype, accessibility-state matrix, Fable-first Claude consensus, implementation review, and scoped Maestro/device protocol. The item-specific conditions below are additional; screenshots or a happy-path render alone cannot close them.

- `UX-AUTH-1`: use Apple Authentication on supported Apple devices and Google Identity on both platforms through backend-verified provider tokens/nonces; never trust profile fields from the client. Linking requires a freshly authenticated existing session or verified ownership of both identities, handles private-relay email and provider-email changes, prevents account takeover/duplicate identity rows under concurrency, supports revocation/deletion obligations, and preserves password login. Cover new account, existing linked account, safe-link conflict, cancel/error/offline, revoked credential, and store-review configuration.
- `UX-ANALYTICS-1`: extend the typed, PII-free event catalog for every paywall trigger/dismissal/result, limit nudge/hit, permission prompt/result, notification open-to-session, flashcard/session completion, and voice lifecycle. Trigger/source IDs are finite enums; anonymous identity aliases once on signup and resets on logout/deletion; events fire once at semantic completion rather than render. Feature flags have typed defaults, exposure events, kill switches, deterministic tests, and no behavior dependency on analytics availability.
- `UX-PAYWALL-1`: one global native-IAP paywall opens over the user's current context from every upgrade trigger and returns there on dismissal. Product title, localized price, period, trial/intro eligibility, renewal terms, restore, privacy/terms, loading/retry, pending, success, cancellation, unavailable-store, and support states come from the store/backend contract; no hardcoded price or invented offer appears. Entitlement success, not purchase-callback optimism, unlocks premium, while repeated taps cannot launch duplicate purchases.
- `UX-LIMITS-1`: expose server-owned plan limits and current usage in the mobile envelope; render honest onboarding caps, visible locked previews, an 8/10-style nudge derived from that envelope, and the hard-limit paywall without closing or clearing the user's task. Dismissal restores draft, selection, scroll, and route context; stale/unknown usage fails conservatively with retry rather than a client magic number. Test exact boundary, concurrent-device consumption, downgrade, offline/stale response, restore/purchase, and accessibility announcement.
- `UX-DEGRADE-1`: every premium/free data surface has explicit loading, empty, stale, retryable-error, offline, rate-limited, locked, and unavailable states where applicable; `null` or hidden controls cannot silently encode entitlement or network failure. A user can distinguish “no data yet” from “upgrade required” and “try again,” with cached age disclosed when relevant and a contextual paywall only for actual entitlement restrictions.
- `UX-STARTER-1`: let a user experience a bounded local interactive demo before authentication, then choose language, proficiency, and a realistic daily goal in a shortened resumable flow. The demo contains no paid/provider call or persisted personal data, back navigation never exits unexpectedly, sign-in paths preserve valid selections, and activation timing is measured from first launch to first completed real quiz without firing completion on an escape link.
- `UX-STARTER-2`: starter packs are curated, versioned, language/level-scoped content with previewable size and provenance; claim/import is bounded, idempotent, quota-aware, and transactional, and never starts duplicate AI jobs. Users can resolve duplicates and partial limit capacity before mutation; retry, cancel, concurrent claim, pack revision, deleted content, and rollback preserve a clear result summary.
- `UX-QUIZ-1`: define a finite session target and stable exit/resume policy, then show a summary derived from accepted server results: attempted/correct, accuracy, box movement, due impact, streak/goal progress, and deterministic next actions. The summary rotates review/paywall prompts only when independently eligible, never traps navigation, handles zero/partial/offline saves, and is fully understandable to screen readers and with animation disabled.
- `UX-STREAK-1`: compute one user-level streak across all wordlists from the `ANALYTICS-1` learning-day contract; show 0 as an invitation rather than hiding it and expose current, longest, daily-goal target/progress, last-qualified day, and at-risk state. Updates are idempotent under repeated answers and cross-device concurrency; timezone changes have an explicit non-exploitative policy and no practice is lost at midnight/DST boundaries.
- `UX-STREAK-2`: freeze and repair have explicit earning, inventory, expiry, application, timezone, and abuse rules and never require payment to preserve an earned streak. Milestones at 3/7/30/100 occur once per crossing, remain accurate after repair/timezone changes, work without motion, and do not obscure the next practice action; rollback and support observability are documented.
- `UX-DUE-1`: render fresh per-wordlist and aggregate due counts, make “Practice N” the primary dashboard action when work is due, deep-link notification taps to the exact authorized list/session, and reconcile app badge count on foreground, answer, notification, logout, and account switch. Stale/offline values are labeled, zero never routes to an empty loop, unauthorized/deleted deep links fall back safely, and create remains discoverable without outranking practice.
- `UX-TABS-1`: implement Home, Practice, Progress, and Profile through Expo Router with one canonical route owner per screen, preserved nested navigation state, stable authenticated deep links, safe-area/keyboard behavior, semantic selected labels, and no duplicate screen stacks. Back behavior, notification/IAP links, account switching, large text, RTL readiness, and returning-user/zero-list states pass on both platforms before old routes are removed.
- `UX-PROGRESS-1`: free users receive truthful current-week goal/streak/practice progress; locked historical/advanced sections remain visible as labeled previews without leaking premium data or pretending dummy values are real. Date range, timezone, cache age, no-data, error, offline, downgrade, and upgrade transitions are explicit, and every upgrade action preserves the selected wordlist/range context.
- `UX-MOTION-1`: answer, session, and streak feedback uses Reanimated for interruptible state-driven motion, optional haptics/sound that respect system/user settings, and a complete reduced-motion/no-sound fallback. Correct and incorrect feedback is not color-only; rapid taps, route changes, backgrounding, and cancellation settle in valid state; Lottie/imagegen assets are finite, licensed/generated for this app, size-budgeted, and do not delay the next action.
- `UX-BULK-1`: paste input is parsed and normalized with a preview of accepted, duplicate, invalid, and over-limit rows before mutation; API batch size/body limits and one idempotency key protect retries. The transaction or documented partial-result contract cannot silently lose rows, duplicate words, exceed quota, or fan out unlimited AI jobs; cancellation and correction preserve the source text.
- `UX-BULK-2`: CSV and Anki import first define supported encodings, delimiters/fields, size limits, duplicate policy, media treatment, and privacy-safe local parsing with a preview/result report. OCR remains a separate owner decision requiring on-device/server data-flow, retention, consent, accuracy, and per-import cost evidence; it cannot be bundled merely because image generation is available.
- `UX-NOTIFY-1`: users explicitly enable reminder categories, local time/days, timezone behavior, and quiet hours before the contextual system permission prompt; Android channels and iOS settings/deep links are configured and recover from denied/provisional states. Preferences are server-owned and multi-device aware, invalid times/zones are isolated, and all messages remain under the global two-per-seven-days daily-plus-due cap unless a later owner-approved policy changes it.
- `UX-NOTIFY-2`: each new streak-risk, enrichment-ready, recap, or win-back type has an eligibility event, dedupe key, expiry, deep-link destination, quiet-hours rule, priority, and cross-type frequency interaction. No notification claims progress or an offer not verified by current state; send retries are idempotent, stale jobs self-cancel, and holdout/opt-out measurement proves incremental value before broad rollout.
- `UX-SHARE-1`: share cards reveal only user-selected milestone/streak/session facts, default to no name/list/content, and preview exactly what leaves the app. Export works offline at bounded resolution/file size, has alt/share text, handles cancellation and unavailable share targets, records no image/content in analytics, and uses approved generated/code-native assets with light/dark and locale-safe layout.
- `UX-WIDGET-1`: first produce separate iOS/Android feasibility spikes covering extension architecture, shared authenticated storage, refresh budgets, stale/offline/privacy states, deep links, and build/signing impact. Implementation exposes only minimal due/streak data, hides it when logged out or privacy-disabled, timestamps staleness, opens the authorized practice route, and requires physical-device plus store-build owner evidence before completion.
- `UX-OFFER-1`: offers render only from currently eligible App Store/Play metadata and backend policy; dismissal count, cooldown, win-back eligibility, code redemption, and experiment assignment are durable/idempotent and never fabricate urgency or discount. No offer interrupts active study, stacks unexpectedly, or changes base-product entitlement semantics; owner-supplied store configuration and measured funnel window remain mandatory.

### Quality milestone acceptance boundaries

- `QA-LOG-1`: test and CI HTTP diagnostics redact authorization and cookie values, fixture emails/tokens, and request or response bodies that can contain credentials while retaining method, normalized route, status, duration, assertion location, and a bounded correlation identifier. Add sentinel-based tests that capture every enabled verbose logger and fail on raw or encoded secret variants; run the exact containerized matrix and scan its complete retained output. Do not disable failed-request diagnostics, lower log levels globally, or hide assertion context merely to make the scan pass.
- `QUALITY-1`: all eight locale catalogs have identical reachable keys, interpolation/plural contracts, and no orphan/dead promise; production code has no hidden English fallback for user-facing copy. Automated parity and hardcoded-copy checks run in CI, touched screens pass locale expansion/pseudolocale review, dead token/theme files are removed only after import proof, and `docs/ui-design-system.md` matches the actual primitives, tokens, motion, contrast, and migration state.
- `QUALITY-2`: every reachable screen passes semantic labels/roles/states, logical focus order, 44-point targets, keyboard/screen-reader navigation, contrast, text resizing without clipping, dark mode, reduced motion, and non-color-only feedback. Automated checks are supplemented by the roadmap's scoped Maestro flows and owner-gated physical VoiceOver/TalkBack, dynamic-type/font-scale, haptic/sound, and iOS/Android device matrix; waivers name the exact platform defect and owner.
- `QUALITY-CODE-1`: capture React Profiler/render-count and bundle/startup baselines for touched activation screens before optimization; set per-screen budgets for avoidable rerenders, list virtualization, style/object churn, and module size. Split god files along state/side-effect/render boundaries with behavioral tests, memoize only measured hot paths, keep Expo Router/data-fetch ownership clear, and prove no regression in typecheck, lint, Jest, Maestro, startup, or interaction responsiveness.

### Security milestone acceptance boundaries

- `AUTH-1`: load immutable typed auth configuration at process startup; reject missing/placeholder/undersized signing material before binding a listener; use the maintained JWT v5 library; accept exactly the configured algorithm (not any HMAC method); validate issuer, audience, environment, subject, issued/not-before/expiry bounds, and token type; issue short-lived access tokens containing only stable authorization identifiers, never email or subscription plan. Existing year-long tokens need an explicit bounded transition/forced-login policy, not silent indefinite acceptance.
- `AUTH-2`: store only hashed refresh credentials, rotate on every use, detect family replay, enforce idle and absolute expiry, and revoke the family on logout, password reset/change, account deletion, or security action. Authorization resolves current account/session state server-side so a deleted, disabled, downgraded, or revoked user cannot retain access through stale claims. Cookie and bearer clients share one session contract without exposing refresh material to JavaScript.
- `AUTH-3`: apply bounded, observable limits to signup, login, reset-request, and reset-consume by both abuse source and normalized account where applicable; return generic account-enumeration-safe responses and `Retry-After`; perform a comparable password-hash operation for unknown-user login; enforce one password policy on every create/reset/change path; and disallow production bcrypt settings below the approved floor even when an environment override exists.
- `HTTP-SEC-1`: use constant-time comparison for configured internal tokens; fail production startup on absent internal-route credentials when those routes are enabled; make CORS allowlists explicit for every environment rather than reflecting arbitrary credentialed origins; add tested CSP/frame/content-type/referrer/permissions headers plus production HSTS; correct cookie seconds/domain/path/secure/SameSite behavior and use a state-changing logout method with CSRF-appropriate semantics; every terminal middleware response calls `Abort` and cannot reach the handler.
- `UPLOAD-1`: cap the HTTP body before JSON/base64 decoding and account for base64 expansion; allow only decoded image magic types, bounded bytes and dimensions, and a server-owned extension/object key; reject traversal/polyglot/unsupported/animated payloads according to a documented policy; avoid per-upload client construction and unnecessary copies; prove failed persistence compensates only the newly uploaded object; retain the existing version-aware account cleanup boundary.
- `API-BOUND-1`: assign explicit maximum IDs/items/page sizes and stable pagination to every currently unbounded list/batch endpoint; reject duplicate/invalid IDs before database work; never serialize password hashes or internal secrets; validate Realtime wordlist ownership; and cap telemetry turns, strings, numeric ranges, and body size before allocation. `REALTIME-1` separately owns trustworthy provider/session usage rather than accepting client totals as billing truth.
- `API-RATE-1`: quota-read/storage errors fail closed with a typed retryable response instead of calling the protected handler; counters reflect committed real reports; cooldown/hour/day boundaries are atomic under concurrency; rejected attempts do not corrupt usage; and tests cover database failure, concurrent submissions, retries, and `Retry-After` semantics.

### Runtime and operations acceptance boundaries

The runtime audit found no liveness/readiness route, a Redis singleton that is permanently poisoned after one failed initialization, an unbounded database startup ping, a detached renewal-reminder goroutine inside River's periodic constructor, no migration lock or statement bounds, no recorded pgBouncer pool-mode proof, per-job `runtime.ReadMemStats`, and a root `alpine:latest` runtime image with no production healthcheck or resource/security defaults. These are separate milestones so a cosmetic container change cannot mask process, database, cache, or scheduling defects.

The current River registration contains 16 job kinds when every conditional surface is enabled: five OpenAI/media jobs, subscription reminders, three push jobs, account cleanup, the periodic no-op, two legacy-provider webhooks, provider-inbox health, Google acknowledgement retry, and its sweep. River 0.23.1 supplies a one-minute job timeout and 25 attempts when this code does not override them. Only the five OpenAI/media workers override timeout; only meaning audio (5), Google acknowledgement retry (18), and account cleanup (an explicit but unchanged 25) override attempts. Meaning audio and Google acknowledgement use argument/queue uniqueness; the other insertions shown in `job_service.go` and `river.go` do not. This is inventory evidence, not approval of those defaults: `ASYNC-2` must classify each side effect and set an intentional policy.

- `API-SHUT-1`: return listener/startup failures through the main lifecycle instead of terminating from a goroutine, handle SIGINT and SIGTERM, stop accepting traffic, and let in-flight requests finish within a five-second production deadline. A deadline/error force-closes remaining connections, returns the shutdown and close failures without bypassing application/Sentry/database defers, and leaves nonproduction behavior explicitly tested. Evidence includes mocked deadline/error branches, a real `net/http.Server` request that proves shutdown waits for its active handler, race repetition, listener-error handling review, and the full API matrix.
- `OPS-HEALTH-1`: complete locally on 2026-08-06. Added public dependency-free `GET /healthz` and database-backed `GET /readyz` directly on the root router outside authentication and the ordinary two-second middleware. Readiness derives a one-second context from the request, returns fixed `200 ready` or `503 not_ready` bodies without serializing database errors, and treats cancellation, pool acquisition/ping failure, and deadline expiration consistently. Five focused tests cover liveness isolation, healthy bounded readiness, secret-bearing database failure, request cancellation, and a real timeout; Fable traced pgx pool semantics, middleware placement, nil-pool reachability, and deployment guidance before returning `APPROVED`. `go test -race ./internal/tests/unit -run 'Test(Healthz|Readyz)' -count=3`, the complete race-enabled unit suite, `go vet`, full lint/format checks, exact containerized `make test`, and diff checks pass. `docs/API_HEALTH_ENDPOINTS.md` assigns liveness to restart decisions and readiness to traffic admission; no production API deployment manifest exists, so `OPS-DEPLOY-1` still owns platform-specific probe wiring and optional probe access-log/tracing suppression.
- `CACHE-1`: do not let `sync.Once` permanently cache a failed Redis initialization; allow concurrency-safe bounded reconnection after transient startup/runtime failure and close/reset without returning a closed singleton. Analytics reads remain correct without Redis, cache writes and invalidation use a live context rather than a canceled errgroup context, every subscription plan follows the same cache contract, and database truth remains authoritative. Tests cover concurrent first use, failed-first-then-success, close/reopen, cancellation, stale data, and cache outage without request failure.
- `ASYNC-1`: remove the renewal scheduler's detached `go func` from River's periodic constructor and the detached analytics refresh path; represent required work as River jobs with explicit args/queue/uniqueness/retry behavior or run bounded work synchronously under the caller lifecycle. Panics become observable errors, shutdown cancels or drains work, duplicate scheduler ticks cannot duplicate user-visible effects, and no goroutine outlives its owning process context silently.
- `ASYNC-2`: inventory every registered job and periodic insertion in one checked table covering queue, maximum attempts, timeout, uniqueness window, cancellation semantics, external paid side effects, transaction boundary, and idempotency key. Defaults are replaced where they permit unbounded or excessive work; every OpenAI/media/store/mail/push side effect is safe across retries and crash-after-side-effect; periodic jobs enqueue durable work rather than doing it inline; and focused tests prove timeout, retry, duplicate, cancellation, and shutdown behavior.
- `OPS-CAP-1`: measure queue latency, execution time, provider latency/rate limits, database connections, CPU, and memory under representative load before changing worker counts. Encode a combined API/worker/replica connection budget below the deployed database/pgBouncer limits, document rollback thresholds, and attach owner-supplied production observations before marking the owner portion complete; guessed concurrency is not acceptance evidence.
- `OPS-DB-1`: identify the deployed pgBouncer pool mode and prove the exact pgx/River query and prepared-statement behavior against that mode. Configure pgx explicitly when transaction pooling is used, keep migrations on a direct PostgreSQL connection rather than the application pgBouncer URL, bound startup acquisition/ping, and document API/worker pool budgets. The local compatibility suite plus owner-supplied deployment configuration and smoke evidence are required for full completion.
- `OPS-MIG-1`: use the direct migration database URL, set bounded `lock_timeout` and `statement_timeout`, acquire a single migration advisory lock, and return typed errors without panic or partial River/application sequencing. Index migrations use a documented non-transactional `CREATE INDEX CONCURRENTLY` procedure where required, with duplicate/preflight, dirty-state, up/down or forward-repair, concurrent-runner, timeout, and rollback evidence. Production execution remains owner-gated.
- `OPS-OBS-1`: remove `runtime.ReadMemStats` from every job-start context or replace it only after a benchmark proves acceptable overhead; retain low-cardinality worker kind/attempt/duration/error signals without job IDs as metric labels or payload/PII capture. Validate observability failure cannot fail the job and document the measurement that justified any retained sampling.
- `OPS-DEPLOY-1`: pin builder and runtime images by approved immutable version/digest, run the three binaries as a dedicated non-root user from a non-home working directory, include only required certificates/timezone/runtime files, and add a production orchestration contract for healthchecks, restart behavior, read-only filesystem/temporary storage, dropped capabilities, no-new-privileges, and CPU/memory limits. Build each executable target, scan the final image, prove API and worker startup/shutdown plus migration invocation, and keep secrets out of image layers/default Compose values; live platform settings and rollout remain owner evidence.

### Store and cutover acceptance boundaries

- `PHASE3-OWNER`: after every local prerequisite including `QA-COVERAGE-1` passes, the owner supplies isolated sandbox-IAP infrastructure and executes the complete Apple TestFlight and Google Play internal-track matrix on physical devices: purchase, pending/grace, renewal, cancel, refund/revoke, restore, invalid evidence, duplicate and out-of-order notifications, acknowledgement, account binding, and entitlement refresh. Store products, localized terms, the Play calendar-month versus four-week decision, Apple V2 notification URL, Google RTDN/IAM/DLQ, review metadata, privacy/account-deletion disclosures, and restore affordance match the tested build. One refundable production smoke purchase per store is preferred; an explicit waiver records the untested production-plumbing risk. No local fixture, emulator, or mocked provider response closes this owner gate.
- `PHASE4-CUTOVER`: re-run the read-only production entitlement audit and prove every legacy record belongs to the controlled store-approval test registry with no real paying, grace, comped, lifetime, manual, or web-only entitlement. With explicit owner authorization, back up the affected schema, stop legacy ingress, drain or deliberately discard each legacy River queue, disable all legacy routes/workers/configuration, deploy native IAP, and prove grant/refresh/revoke plus web checkout-disabled behavior under observation and rollback thresholds. Only after the stable observation gate may forward migrations and code commits delete Stripe/RevenueCat tables, fields, dependencies, environment variables, legal copy, SDKs, and rollback scaffolding; API and workers must then boot without legacy credentials and the full API/mobile/web/store validation must pass. No existing-payer migration is required because the zero-real-entitlement gate is a prerequisite, but test-account purchase flows remain covered.

## Phase 0 — Scope and zero-entitlement gate

This phase is read-only except for documenting decisions. Nothing that drops provider code or schema may start before it passes.

- [x] Query production subscription state for Stripe and RevenueCat: active, trialing, past_due, grace-period, cancelled-but-entitled, and any other state that grants access.
- [x] Check for manual, comped, lifetime, promotional, or feature-flag entitlements outside the subscription tables.
- [x] Check whether web users can currently hold premium state independently of those providers.
- [x] Record the result as a dated, non-secret evidence note: `zero paid/entitled users confirmed`, or stop with `[!]` and owner direction.
- [x] Define the single test-account classification used by both Phase 0 and Phase 0.5: only owner-confirmed store-approval accounts listed in the controlled test registry qualify; names, email patterns, or subscription status alone are not sufficient.
- [x] Confirm web behavior: no web purchase flow; mobile-store entitlements may be honored on web if the shared account contract supports it.
- [x] Replace the contradictory sequence in the strategy with this dependency order.

Gate 0 acceptance:

- A production query/evidence artifact proves zero real customer entitlements across all known sources, with store-approval test records explicitly classified and excluded from customer migration handling.
- If any entitlement cannot be classified under the shared test-account definition, all dependent work stops and the finding is presented for owner resolution.
- Product sign-off explicitly accepts disabling web checkout.
- Product policy is fixed below; exact store availability and localized commerce metadata are verified from the stores in Phase 3 rather than copied into application code.

Product decision contract:

| Decision | Contract |
| --- | --- |
| Products | Expected Apple monthly `decorebator_monthly_premium1` and annual `decorebator_annual_premium`; Google short-period `p1m` and annual `p2m`. Phase 3 must reconcile these IDs against each live store catalog and the backend allowlists before building. |
| Entitlement and renewal | Both products grant backend-owned `premium` and auto-renew while enabled. The intended short period is calendar-monthly, but live Google `p1m` currently reports `P4W`; Gate 3 must either change it to a monthly base plan or explicitly approve four-week billing and update every contract/UI expectation. Annual is one year. Current provider state controls access. |
| Pricing and offers | StoreKit/Google ProductDetails are the only source of localized price, currency, billing period, and introductory terms. No hard-coded price or unsupported offer is a fallback. |
| Countries/storefronts | Sell only in storefronts where the corresponding product is approved and returns current localized metadata. Phase 3 records the actual Apple/Google availability matrix; an absent product is unavailable, never synthesized. |
| Free tier | One wordlist with at most ten words; premium removes those two limits. Any future limit change is an explicit product decision, not inferred from store metadata. |
| Web | The website has no checkout and its premium CTA remains a download link. A future authenticated web client may honor the same backend entitlement but must not add a separate purchase authority. |

Phase 0 status:

- [x] **2026-08-04 — zero real paying users confirmed; test-account exception recorded.** A read-only query through `api/.env.prod` connected as `doadmin` to `defaultdb` and found 6 subscription rows: 3 Stripe rows currently `active` (1 monthly and 2 annual, with period ends in 2026–2027) and 3 historical RevenueCat rows marked `cancelled`; the `users` table contains 3 active non-free users plus 1 cancelled annual user. The owner identified the active accounts as store-approval test accounts, not real customers. No user IDs, payment identifiers, or secrets were written to this plan, and no production data was changed. Customer migration/grandfathering is therefore out of scope. Before Phase 4 schema replacement, preserve or explicitly re-seed these approval accounts against the Apple/Google test workflow and verify that test records cannot be mistaken for real customer entitlements. The earlier Phase 0 block is cleared for Phase 1; the test-account lifecycle remains a cutover acceptance item.
- [x] **2026-08-06 — Phase 0 tracker reconciled.** The dated production snapshot above, the repository schema/access-path audit, and the owner's classification cover the seven prerequisite tasks: premium authority was limited to legacy subscription/user state or RevenueCat's active entitlement check, with no manual/comped/lifetime/promotional/feature-flag grant path found; the web pricing CTA links to app download and holds no independent premium state; and `docs/fixtures/store-approval-accounts.json` is the sole controlled exception registry. `docs/queries/run-release-entitlement-audit.sh` now supplies the separate release-window recheck, so this historical Gate 0 completion must not be read as current production-cutover evidence. The strategy phasing was superseded with the dependency sequence below, and the product decision contract records the fixed policy while leaving live catalog/localization proof to Phase 3. Fresh Claude review started with Fable; it found one remaining `phases 0–2` pointer in the strategy overview, which was corrected to the Gate-3-before-removal sequence, and the same Fable reconciliation returned `APPROVED`.

## Phase 0.5 — Activation baseline and measurement gate

The production audit found that sign-ups are reaching wordlist creation but rarely reaching the learning loop. This phase makes that funnel measurable before the corresponding UX work is evaluated. It is not satisfied by total sign-up or payment counts, and the baseline is measurement-only: poor activation does not silently halt the roadmap, but it determines UX priority and the owner-approved release target.

Baseline captured on 2026-08-04, using read-only production data and excluding the four owner-confirmed store-approval accounts:

- 33 non-test users total.
- 26 created at least one wordlist; 14 added at least one word; 3 attempted at least one quiz.
- Last 30 days: 4 sign-ups, 3 wordlist creators, 2 users adding words, 0 quiz users.
- Last 90 days: 6 sign-ups, 5 wordlist creators, 4 users adding words, 0 quiz users.
- Last 365 days: 32 sign-ups, 26 wordlist creators, 14 users adding words, 3 quiz users.
- The database does not measure app opens, screen impressions, failed client flows, or session completion reliably; those require the event contract below.

Required work:

- [x] Keep a stable, non-production-secret registry/fixture for store-approval accounts so they are excluded from activation reporting without relying on ad-hoc user IDs.
- [x] Define canonical events and properties: `user_signed_up`, `wordlist_created`, `word_added`, `quiz_session_started`, `quiz_session_completed`, `quiz_answered`, and `practice_cta_opened`. Specify one event per semantic action, session identity, source/entry point, wordlist context, and failure/error outcome.
- [x] Audit existing mobile/PostHog events before adding duplicates. Correct the current mismatch where `quiz_completed` represents individual answers rather than completed sessions.
- [x] Add a development/dry-run sink and tests proving event names, required properties, deduplication, and exclusion of raw word/content data.
- [x] Produce the reproducible pre-release database cohort report for 7/30/90/365-day sign-ups with signup→wordlist, signup→word, and signup→database quiz-answer conversion. Keep its quiz-answer definition explicitly separate from completed sessions.
- [b] Produce the post-instrumentation production PostHog comparison for signup→wordlist, signup→word, signup→first quiz, and first quiz→completed session; keep the database baseline alongside it and do not compare unlike definitions.
- [b] Define owner-approved activation targets and the observation window before the final release gate. Do not invent a target percentage or declare success from a local test run.

Phase 0.5 implementation-readiness gate — complete; unblocks Phase 1 and independent lanes:

- The activation funnel can distinguish real users from store-approval accounts and can be queried by signup cohort and app version.
- Tests prove that a quiz answer does not count as a completed session and that retries/background duplicates do not inflate conversion.
- The event contract, controlled registry, migrated call sites, validation/dedupe tests, and reproducible pre-release database baseline are complete.
- PostHog React Native supplies `$app_version` from the installed Expo application metadata, so production cohorts can be segmented by app version without raw user/content properties.

Final-release measurement gate — open; blocks final release only:

- A production PostHog dashboard or reproducible export shows post-instrumentation values for all four funnel transitions alongside the non-equivalent database baseline.
- The owner has chosen the activation success threshold and observation window.

Phase 0.5 progress:

- **2026-08-04 — canonical activation contract defined.** Added the typed, privacy-safe event contract and dry-run capture boundary in `mobile/utils/activationEvents.ts`, with unit coverage for canonical names, scalar allowlisting, raw-content rejection, falsy values, and dry-run behavior. Documented session semantics and allowed properties in `mobile/docs/posthog-events.md`. The production PostHog comparison and owner target selection remain final-release measurement work.
- **2026-08-04 — controlled store-approval registry prepared.** Added `docs/fixtures/store-approval-accounts.json` with the four owner-confirmed internal user IDs from the read-only subscription audit, provenance, re-verification date, and usage rules in `docs/fixtures/README.md`. The registry artifact excludes accounts by explicit numeric ID and contains no email, payment, or secret data; production report consumption remains a separate pending item.
- **2026-08-04 — existing activation events audited and migrated.** Removed duplicate `signup_completed` and raw email PostHog properties, removed the raw wordlist name, added `word_added`, and replaced per-answer `quiz_completed` with session-aware `quiz_session_started`, `quiz_answered`, and `quiz_session_completed` events. Quiz counts are ref-backed for cleanup accuracy, and Sentry sign-in context no longer stores email. Targeted lint, formatting, and activation tests pass; full typecheck remains blocked by pre-existing WebRTC event-typing errors, and the full Jest suite retains unrelated Expo/mock/API-environment failures. No bulk word-add path was found; the production PostHog comparison remains final-release measurement work.
- **2026-08-04 — activation dry-run/validation sink completed.** `mobile/utils/activationEvents.ts` now enforces event-specific required properties, rejects invalid captures before sending, supports caller-owned process-scoped dedupe keys, and keeps dry-run captures from mutating real dedupe state. Seven unit tests cover names, required fields, privacy filtering, falsy values, dry-run behavior, dedupe, and session IDs. Production PostHog delivery remains intentionally non-durable; the production comparison remains a final-release measurement gate.
- **2026-08-04 — database cohort baseline automated; analytics comparison blocked.** Added the read-only `docs/queries/run-activation-cohort-report.sh` and SQL query, which loads the controlled registry and reports 7/30/90/365-day cohorts without hardcoded test IDs. The current run produced 7d `0/0/0/0`, 30d `4/3/2/0`, 90d `6/5/4/0`, and 365d `32/26/14/3` for signups/wordlists/words/database quiz answerers; the query labels database quiz answers as non-equivalent to completed sessions. `[b] BLOCKED — owner action: provide an authenticated production PostHog read/export credential or dashboard export, then rerun the post-instrumentation cohort comparison and record the observation window. The database-only baseline is complete; no production data was changed.`
- **2026-08-04 — Phase 0.5 gate split corrected after Claude reconciliation.** The implementation-readiness gate is met, so Phase 1 and independent UX/reliability work may proceed. `[b] BLOCKED — owner action before final release: provide an authenticated production PostHog export and choose the activation thresholds plus observation window. These owner actions block final release measurement sign-off only, not Phase 1.`

## Phase 1 — Design the store-neutral entitlement contract

Keep this contract independent of SDK names. Do not delete the current providers yet.

- [x] Define canonical subscription fields: account/user, store (`apple` or `google`), product ID, entitlement, status, start/end dates, cancellation/revocation state, environment, and last verified time.
- [x] Define Apple identity binding using an app-account identifier and original transaction identifier; define Google binding using a verified purchase token and an obfuscated account identifier. Never trust a client-supplied user ID without server binding.
- [x] Define event idempotency keys and unique constraints for Apple transaction/notification IDs and Google purchase tokens/event IDs.
- [x] Define server operations for purchase verification, restore/status refresh, and provider notification ingestion. Specify retry, pending, duplicate, invalid, revoked, and unknown-product behavior.
- [x] Define the mobile API response consumed by settings/paywall: products, current entitlement, pending state, error state, and restore result.
- [x] Select and spike an Expo-compatible native IAP implementation for SDK 54. Confirm that it works with the existing native projects and development-client workflow before committing to it.

Phase 1 progress:

- **2026-08-04 — canonical entitlement lifecycle complete.** Added the server-owned `StoreEntitlement` model and `docs/IAP_ENTITLEMENT_CONTRACT.md` with Apple/Google normalization, cancellation-versus-expiration semantics, fail-closed access rules, environment separation, and explicit deferral of provider identity/event persistence to the following items. Test-first evidence covers every enum, required fields, invalid periods/revocation, cancellation during a paid period, missing/expired/exact-boundary period ends, and access for each status. Claude `fable` identified deterministic pending-cancellation, fail-closed nil-period, and scope-separation issues; all were reconciled and the second `fable` review returned `APPROVED`. `make test-unit`, race-enabled unit tests, `make format-check`, changed-file lint, and `git diff --check` pass; targeted coverage reports 100% for every function in `entitlement.go`. The repository's aggregate model lint still reports only pre-existing findings in untouched `word.go`, `definition_image.go`, and legacy `subscription.go`, while the two changed Go files pass file-scoped `golangci-lint`.
- **2026-08-04 — store-to-user identity binding complete.** Added internal, non-serializable Apple/Google account and verified-purchase identity models plus the pre-purchase, verification, restore, and notification trust rules in `docs/IAP_ENTITLEMENT_CONTRACT.md`. Tests prove exact server-issued identifier matching, malformed/mismatched fail-closed behavior, Google’s 64-character limit, environment/verification requirements, and JSON non-exposure for all four identity records. Claude `fable` returned `APPROVED`; its non-blocking serialization-test suggestion was added, while structured log redaction is recorded for the later verification/operation item where logging is introduced. `make test-unit`, race-enabled unit tests, patch-scoped model lint, unit-package lint, `make format-check`, and `git diff --check` pass; targeted coverage reports 100% for every function in `entitlement_identity.go`.
- **2026-08-04 — provider idempotency and uniqueness contract complete.** Added length-prefixed SHA-256 key builders with type-separated mutable `ProviderRecordKey` and deduplicating `ProviderEventKey` results, plus the required account, transaction, purchase, transport, and semantic-event uniqueness shapes. Google RTDN event keys intentionally omit environment until the Play refresh supplies it; Apple notification UUID and topic-scoped Pub/Sub message IDs deduplicate transport while semantic keys suppress equivalent RTDN envelopes. Claude `fable` found the original record/event type conflation, unavailable pre-refresh Google environment, omitted Apple transaction constraint, and error-style issue; all were corrected and the second `fable` review returned `APPROVED`. `make test-unit`, race-enabled unit tests, patch-scoped model lint, unit-package lint, `make format-check`, and `git diff --check` pass; targeted coverage reports 100% for every function in `entitlement_idempotency.go`.
- **2026-08-04 — verification, restore, and notification operation contract complete.** Added stable applied/unchanged/pending/duplicate/retry/rejected outcomes, permanent and retryable result codes, fail-closed access gating, and atomic verify/restore/inbox workflows. Ordering compares provider occurrence/version only with stored provider occurrence/version; older and equal events are no-ops, unknown statuses are permanent invalid-purchase rejections, and newer revocations apply immediately. Claude `fable` found the original mixed-clock comparison, equal-version reapply, unknown-status retry ambiguity, and high-risk test gaps; all were corrected and the second `fable` review returned `APPROVED`. `make test-unit`, race-enabled unit tests, patch-scoped model lint, unit-package lint, `make format-check`, and `git diff --check` pass; targeted coverage reports 100% for every function in `entitlement_operation.go`.
- **2026-08-04 — mobile IAP envelope contract complete.** Added server-allowlisted single-store `premium` products, a narrow Apple/Google pre-purchase account context, current entitlement, pending, safe errors, restore outcomes, and server time, with explicit null-state JSON and no user IDs or provider purchase evidence. StoreKit/Google ProductDetails remain authoritative for localized price/title/offer data, while only backend entitlement state grants access. Claude `fable` found duplicate not-requested restore encodings, context/catalog nullability ambiguity, and permanent-error/pending conflicts; all were corrected and the second `fable` review returned `APPROVED`. `make test-unit`, race-enabled unit tests, patch-scoped model lint, unit-package lint, `make format-check`, and `git diff --check` pass; every function in `mobile_iap.go` has at least 86% targeted coverage.
- **2026-08-04 — Expo SDK 54 native IAP implementation selected and compiled.** Selected and pinned `expo-iap` 5.0.1 after comparing it with `react-native-iap` 16.0.2; the Expo Module fits the existing SDK 54/New Architecture development client without the latter's additional Nitro Modules runtime. Added a type-checked contract spike for product fetch, Apple/Google account-bound purchase, restore, and backend-gated transaction completion. An isolated `expo prebuild` generated both platforms, and Android `assembleDebug` compiled all 609 tasks and produced a 227 MB development-client APK with OpenIAP and Play Billing; iOS generated a 15.1 target with OpenIAP CocoaPods integration, while actual Xcode/store execution remains a Phase 2/3 gate. Claude `fable` independently checked the installed 5.0.1 API/types, security boundary, dependency/plugin fit, evidence claims, and Gate 1, then returned `APPROVED`; its optional synchronous-rejection and three-day Google acknowledgement notes were incorporated. Full evidence and reconsideration triggers are recorded in `docs/IAP_NATIVE_LIBRARY_SPIKE.md`.

Gate 1 acceptance — complete:

- Contract review confirms that the server, not the client, grants entitlement.
- Duplicate purchase and duplicate notification requests are safe and produce one entitlement transition.
- The selected library can be built in an Expo development client and has a path to App Store/Play test environments.

## Phase 2 — Implement Apple and Google IAP while old providers are untouched

### Backend

- [x] Implement Apple purchase verification using signed transaction data/JWS and the App Store Server API.
- [x] Implement Apple App Store Server Notifications V2 with production and sandbox environment handling, signature validation, replay protection, and atomic event idempotency.
- [x] Implement Google purchase verification through the Play Developer API.
- [x] Handle Google pending purchases and acknowledgement; do not grant access from an unverified client callback.
- [x] Implement Google Real-time Developer Notifications, Pub/Sub authentication, redelivery handling, and atomic event idempotency.
- [x] Implement authoritative Apple subscription-status refresh and normalization; transaction verification alone must never derive entitlement lifecycle state.
- [x] Persist canonical Apple/Google entitlement and store-identity bindings with atomic verified-event application, encrypted/protected provider evidence, account/token lookup for restore and notifications, and no dependency on Stripe/RevenueCat rows.
- [x] Add store-specific secrets/configuration with startup validation and safe error reporting; wire the RTDN HTTP endpoint with `http.MaxBytesReader`, explicit disposition-to-status mapping, structured rejection/retry logs and metrics, Pub/Sub dead-letter configuration, and a reaper or alert for stranded retryable inbox rows.
- [x] Expose authenticated, store-neutral mobile IAP orchestration: server-owned catalog/context, direct Apple/Google verification followed by authoritative lifecycle refresh and persistence, bounded non-claiming restore with per-evidence outcomes, exact server-derived environment reads, durable Google acknowledgement retry, provider-call abuse controls, and an effective-access bridge that makes verified store entitlement consistent across limits, workers, quiz behavior, analytics caching, and profile responses while legacy providers coexist.
- [x] Add fixture-backed unit and integration lifecycle evidence for grant, renew, cancel, grace/pending, refund/revoke, restore, invalid receipt, duplicate receipt, duplicate notification, and out-of-order notification.
- [ ] Complete `QA-COVERAGE-1`: repair canonical unit-test discovery and raise real application-package coverage to the unchanged 70% unit/80% integration thresholds before Gate 3 closes.

Phase 2 progress:

- **2026-08-04 — Apple transaction verification boundary complete.** Added a bounded App Store Server API `Get Transaction Info` client with five-minute ES256 authorization, production-first lookup, and sandbox fallback only for Apple's documented `4040010`; Apple retryable error codes remain retryable independently of HTTP status. Added a Go-native signed-transaction verifier matching Apple's published ES256/x5c profile: exactly three certificates, pinned Apple root, Apple leaf/intermediate OIDs, certificate validity at `signedDate`, and a raw P-256 signature check. The purchase verifier then enforces authenticated account binding, exact app-account UUID, bundle, environment, transaction ID, auto-renewable product type, and the server product allowlist before returning redacted transaction evidence. It deliberately does not derive entitlement status or auto-renew state from transaction dates; verified renewal JWS from Get All Subscription Statuses is required before the later atomic entitlement operation. Claude review began with `fable` and used the allowed temporary `opus` fallback; it found status overreach, Apple retry-code, malformed-ID, and adversarial-test gaps. All were corrected, and the final `fable` reconciliation returned `APPROVED`. Targeted race tests, changed-file lint, `go vet`, formatting, and diff checks pass; no route, persistence mutation, or production provider call is enabled by this item.
- **2026-08-05 — Apple App Store Server Notifications V2 verified ingestion complete.** Added outer V2 and nested transaction/renewal JWS verification with production/sandbox app binding, UUID replay keys derived only after outer verification, payload-type discrimination, product/lineage/environment checks, a five-minute future-skew guard, and bounded request size. Migration `000074` adds the provider-event inbox with explicit store/environment/result/state constraints, transport and semantic uniqueness, attempt counters, retry timestamps, and renewable leases. Ingestion now durably claims a short lease, performs provider refresh without holding a database transaction, and atomically commits only the local entitlement mutation with the final inbox result; verified-envelope nested failures are safely recorded as rejected, transient refreshes become retryable, and stale workers cannot complete after losing their lease. Review started with `fable` (temporarily unavailable at its usage limit), then Claude `opus` found seven blocking conflict, retry, transaction-duration, type-confusion, timestamp, schema, and concurrency-test gaps; all were corrected and the same `opus` reviewer returned `APPROVED`. `make test-unit`, focused race-enabled PostgreSQL integration tests including an eight-way concurrent claim, migration `74` down/up, changed-file lint, `go vet`, formatting, and diff checks pass. The later handler/worker item must nack retry outcomes, add its poison-event/dead-letter policy, and may move lease timing fully to database time; no route, live migration, or provider call is enabled here.
- **2026-08-05 — authoritative Apple subscription-status refresh complete.** Added the App Store Server API `Get All Subscription Statuses` client and normalization across active, expired, billing-retry, grace, and revoked states, with exact environment, app, account, product, transaction-lineage, signed-transaction, and signed-renewal binding. Provider status is preserved without clock-derived lifecycle rewriting; signed revocation evidence is restrictive, cancellation is represented as snapshot observation, unknown future statuses retry, optional future renewal products remain forward-compatible, and transaction verification alone still cannot grant lifecycle state. Review started with `fable` (temporarily unavailable at its usage limit), then Claude `opus` found eight product, lifecycle-clock, event-order, revocation, cancellation, forward-compatibility, and test blockers; all were corrected and the same `opus` reviewer returned `APPROVED`. Focused race tests, changed-file lint, `go vet`, formatting, and diff checks pass. Persistence must keep snapshot timestamps distinct from notification occurrence ordering, retain the first cancellation observation instead of moving it forward on every refresh, and alert on indefinitely retrying unknown future statuses; no route, persistence mutation, or production provider call is enabled here.
- **2026-08-05 — canonical store entitlement and identity persistence complete.** Migration `000076` adds provider-independent entitlement, account-identity, and purchase-binding tables with strict user/store/environment composite foreign keys, current-token uniqueness, separate snapshot/event cursors, and an explicit retained-revocation result. Recoverable provider identifiers use AES-256-GCM with immutable owner/type AAD and versioned keys; stable HMAC blind indexes support restore/notification lookup without plaintext storage, and bounded re-encryption plus old-key row counts gate key retirement. Direct purchase and inbox paths share one savepoint-protected atomic apply operation, typed ownership conflicts, restrictive lifecycle merge, first-observation cancellation cycles, superseded-token suppression, concurrent upgrade serialization, and pending post-revocation candidate recovery. Review started with `fable` (temporarily unavailable at its usage limit), then Claude `opus` found six ownership, supersession, revocation, transaction, stale-metadata, and constraint-classification blockers plus two residual historical/pending-repurchase traps; all were corrected and the same `opus` reviewer returned `APPROVED`. Unit/race tests, 10 focused PostgreSQL integration tests, provider-inbox result coverage, migration `76` down/up, build, vet, and formatting checks pass. The repository-wide integration harness remains independently broken: `docker-compose.test.yml` pins Go 1.23 while `go.mod` requires 1.25, and migration `000052` drops River tables while `CleanTestData` still truncates `river_job`; these test-infrastructure pendencies do not affect the focused persistence suite and must be repaired before relying on the one-command full-suite gate.
- **2026-08-05 — Google Play subscription verification complete.** Added a bounded `purchases.subscriptionsv2.get` client with bearer-token injection, exact v2 URL encoding, HTTPS-only remote transport, loopback-only HTTP test support, a request-level 15-second timeout independent of the injected client, a one-MiB response limit, and safe retry classification including Google's documented `409 concurrentUpdate`. Verification requires the authenticated user’s exact server-issued obfuscated account ID, one allowlisted premium line item with a valid auto-renewing/prepaid plan union, recognized subscription and acknowledgement states, the documented body `etag`, coherent provider timestamps, and test-purchase environment isolation. It normalizes pending, active, paused, grace, hold, canceled, expired, and pending-canceled states; tolerates five minutes of expiry clock skew; carries linked-token replacement as an explicit superseded/expired lineage; and never serializes raw purchase tokens. Review started with `fable` (temporarily unavailable at its usage limit); Claude `opus` found timeout/transport, expiry, cancellation-union, transient-body, and adversarial-test gaps, which were corrected. Its claim that `etag` was absent was reconciled against Google's current primary resource documentation, and `opus` withdrew that finding and returned `APPROVED`. `make test-unit`, focused race tests, changed-file lint, `go vet`, formatting, and diff checks pass. OAuth credential construction, acknowledgement mutation, persistence, and live Play calls remain in their explicit later items; no route or provider call is enabled here.
- **2026-08-05 — Google pending-purchase and acknowledgement processing complete.** Added a verify-first processor that never grants from a client callback: pending, expired, and revoked purchases are not acknowledged, while verified paid-lifecycle purchases use the exact Play acknowledgement endpoint and provider product/token evidence. Explicit acknowledgement outcomes preserve the verified purchase across retryable failures, distinguish local request errors from provider failures, and re-verify permanent failures to resolve idempotent acknowledgement races; unknown access-token failures fail safely as retryable unless a credential adapter classifies them permanent. The client applies bounded reads, request timeouts, URL escaping, bearer authentication, safe errors, and retry classification. Review restarted with `fable` (temporarily unavailable at its usage limit), then Claude `opus` found six blocking retry, error-classification, local-validation, token-source, failure-test, and wiring-boundary concerns. All implementation findings were corrected; the same reviewer accepted that route/config wiring and the mobile durable retry queue belong to their explicit later items and returned `APPROVED`. Race-enabled unit tests, changed-file lint, `go vet`, formatting, and diff checks pass; no route, credential, persistence mutation, or live Play call is enabled here.
- **2026-08-05 — authenticated Google RTDN ingestion complete.** Added Google-signed OIDC token verification with audience, issuer, service-account email, and verified-email binding; exact topic/subscription/package scoping; bounded Pub/Sub/base64/JSON parsing; transport and semantic replay keys; subscription, forward-compatible future subscription type, voided-subscription, test, unsupported-product, and poison-event handling; and a redacted event representation. Provider refresh runs outside the database transaction under a 20-second deadline inside the 30-second inbox lease. Permanent verification failures complete atomically as rejected, transient failures persist retry state, non-terminal duplicates explicitly require redelivery, and every failure exports an acknowledge/retry/unauthorized disposition for the later HTTP handler. Migration `000075` adds a distinct accepted test-event result and safely remaps it during rollback. Review restarted with `fable` (temporarily unavailable at its usage limit); Claude `opus` found eight blocking duplicate-redelivery, failure-classification, timeout, forward-compatibility, void/refund, rollback, poison-envelope, and dependency-tidiness issues. All were corrected, the stale type-list claim was reconciled against Google’s current primary documentation, and the same reviewer returned `APPROVED` twice after the final disposition API hardening. Full race-enabled unit tests, real PostgreSQL inbox integration tests, migration `75` down/up, changed-file lint, `go vet`, formatting, module tidiness, and diff checks pass. The next configuration/wiring item owns the HTTP body limiter and status mapping, credentials/startup validation, structured logs/metrics, Pub/Sub DLQ, and stranded-retry reaper or alert; no route, live migration, credential, or provider call is enabled here.
- **2026-08-05 — store IAP startup, webhook, and operations wiring complete.** Added production fail-closed typed configuration for Apple credentials and pinned roots, Google Application Default Credentials and exact Pub/Sub bindings, premium product catalogs, protected-evidence keyrings, and the expected dead-letter topology; disabled environments expose no store webhook routes. The composition root now wires Apple and Google clients, verifiers, authoritative refreshers, blind-index binding resolution, canonical persistence, and authenticated notification ingestors. Apple and Google endpoints apply `http.MaxBytesReader`, a 28-second request deadline, explicit terminal/authentication/retry status mapping, typed permanent-failure handling, bounded non-sensitive `error_kind` logs, capped delivery-attempt counters, and a static-authenticated per-process metrics view. Google void notifications revoke only exact current primary bindings, apply at equal cancellation timestamps, and cannot revoke historical or linked replacement tokens. A River health job alerts on both overdue retryable rows and expired processing leases without pretending to replay deliberately unstored payloads; `docs/GOOGLE_PLAY_RTDN_OPERATIONS.md` records the no-mutation DLQ/IAM/monitoring runbook. Review restarted with `fable` (temporarily unavailable at its usage limit); Claude `opus` found equal-timestamp refund and undiagnosable retry-log blockers, both were corrected, its three configuration notes were adopted, and the same `opus` thread returned `APPROVED`. The complete race-enabled unit suite, build, `go vet`, formatting, changed-file lint, diff checks, focused provider-inbox PostgreSQL tests, and equal/stale/historical/linked Google void integration tests pass. `make coverage-threshold` remains an independently broken repository gate: `make test-unit` succeeds but omits `-coverpkg` for the external `internal/tests/unit` package, producing a 0% no-statements profile instead of measuring application code; the next explicit test-matrix item owns repairing that instrumentation without lowering the 70% threshold. No live cloud resource, provider call, production migration, or external route configuration was changed; routes remain inactive until operators explicitly enable valid IAP configuration.
- **2026-08-05 — authenticated IAP orchestration and effective-access bridge complete.** Added authenticated context, Apple verify, Google verify, and bounded restore routes in a dedicated 25-second group outside the legacy two-second timeout; disabled mode returns the same valid retryable envelope. Typed monthly/annual catalogs now derive provider allowlists, and `STORE_IAP_CLIENT_ENVIRONMENT` pins every client verify/read/restore to one server-selected environment: production never probes sandbox, while TestFlight uses a separate sandbox backend. Restore rejects rather than claims unbound/foreign evidence, collapses ownership-sensitive errors, deduplicates Apple lineage refresh, exposes only client request indexes, incorporates protected server-known current bindings into aggregate outcomes, bounds and budgets provider calls, and makes no new provider call after cancellation. Google access additionally requires acknowledgement; transient failures persist unacknowledged state, enqueue unique evidence-free River retries, and are repaired by a five-minute bounded sweep that attempts every candidate and alerts at 24/60 hours. Redis-backed per-user budgets and a process-local in-flight guard protect provider calls; production fails startup without configured Redis, while nonproduction has bounded stale-evicting local fallback. Exact-environment canonical access is additive with still-valid legacy access and now drives subscription limits, worker eligibility, quiz selection, analytics/error-limit request behavior, and profile projection without writing fake legacy plans. Review began with `fable` (temporarily unavailable at its usage limit), then Claude `opus` found five restore/retry blockers and one empty-restore regression; all six were fixed with dedicated tests, and the same `opus` thread returned `APPROVED`. Race-enabled unit tests, focused real-PostgreSQL store/access tests, all non-integration packages, build, `go vet`, formatting, changed-file lint, and diff checks pass. `make test-unit` also passes, but `make coverage-threshold` still reports the pre-existing `0% [no statements]` profile because the external unit package lacks `-coverpkg`; the immediately following matrix item remains responsible for repairing that command without lowering 70%. No live provider call, cloud mutation, production migration, push, or release occurred.
- **2026-08-05 — lifecycle matrix complete; aggregate coverage debt exposed.** Added `docs/IAP_LIFECYCLE_TEST_MATRIX.md`, which maps every required grant, renew, cancel, grace/pending, refund/revoke, restore, invalid-receipt, duplicate-receipt, duplicate-notification, and out-of-order case to exact fixture-backed unit or PostgreSQL integration evidence. A new persistence integration test proves that repeating one verified purchase is an unchanged no-op with one entitlement/binding and that a newer renewal advances the same row. Added `-coverpkg=./internal/...` consistently to local, scripted, report, and GitHub unit commands and to the external integration workflow, without changing the 70%/80% thresholds. The repaired race-enabled unit run passes all tests and reports the real repository-wide statement baseline of 19.7%, so `make coverage-threshold` now fails honestly instead of passing through a meaningless 0% no-statements profile. Review restarted with `fable` (temporarily unavailable at its usage limit); `opus` traced the new test through persistence, verified every matrix reference and coverage entry point, confirmed the pre-existing disabled CI threshold job was not changed, and returned `APPROVED`. The 2026-08-06 catalog reconciliation supersedes the former owner-decision classification: `QA-COVERAGE-1` now owns the executable work to meet the unchanged 70%/80% policy without exclusions or denominator changes.
- **2026-08-05 — native-IAP mobile purchase tranche complete.** The Fable-approved prototype and decision record under `docs/mockups/native-iap-paywall/` now back one reachable Settings paywall with explicit no-default selection, backend/store catalog intersection, StoreKit/Play-authored title and commerce terms, per-user Apple introductory-offer eligibility, separate introductory and recurring terms, account-bound purchase requests, backend-authoritative verification, pending/deferred handling, backend-gated transaction completion, bounded restore, replay queueing, duplicate callback/rejection suppression, existing-entitlement display, foreground/deep-link refresh, and all six privacy-safe funnel events. Stripe checkout, RevenueCat modal selection, locale/platform provider routing, hard-coded prices, and provider-shaped upsell hints are removed from reachable mobile behavior; rollback-era files remain unreferenced until the Phase 4 deletion. The warm study-shelf motif is implemented with Reanimated 4 and a static reduced-motion state; eight locales, 44pt controls, live state announcements, large-text wrapping/scroll coverage, retry/restore/legal recovery, dark-theme roles, and no raster asset preserve the approved design contract. Fresh review started with `fable`; four same-session reconciliation rounds corrected the native-sheet foreground race, replay-before-context handling, ineligible intro offers, intro-versus-renewal labeling, scaled-text coverage, false already-entitled activation, cancellation's dual promise/listener path, and deferred-payment analytics before Fable returned `APPROVED`. Focused IAP/paywall/Settings suites, the complete mobile Jest suite, TypeScript, repository-wide lint, formatting/diff checks, locale-shape validation, and Android production export pass. Real sandbox/TestFlight and Play internal-track purchases, store notifications, cancellation/refund/revocation, and release metadata remain explicitly owned by Phase 3; `QA-COVERAGE-1` remains a separate executable local prerequisite before Gate 3 closes.

### Mobile

- [x] Add the selected IAP native dependency and rebuild the development client.
- [x] Replace the settings paywall purchase path with the store-product API; show store-localized price and legal text from the store/product contract.
- [x] Implement purchase, pending, failure, restore, and “already entitled” states.
- [x] Emit the typed privacy-safe `paywall_impression`, `paywall_plan_selected`, `purchase_pending`, `purchase_succeeded`, `purchase_failed`, and `restore_completed` events from those native-IAP states; never attach receipts, transaction IDs, purchase tokens, localized price text, or raw provider errors.
- [x] Remove platform/locale routing that selects Stripe or RevenueCat.
- [x] Ensure purchase state refreshes on app foreground and after notification/deep-link entry.
- [x] Keep the old provider path disabled behind a temporary rollback flag only if needed for the first release; it must not be reachable in the IAP-only product behavior.

Gate 2 acceptance:

- Backend tests prove verification and idempotency without live provider calls.
- Mobile tests cover loading, success, pending, cancellation, failure, restore, and retry states.
- No new code grants premium from price/product metadata or a client-only success callback.

## Phase 3 — Store test matrix and production plumbing gate

- [b] Apple sandbox/TestFlight: new purchase, restore, renewal, cancellation, grace-period behavior, refund/revocation, invalid transaction, duplicate notification, and out-of-order notification.
- [b] Google Play internal track: new purchase, acknowledgement, pending purchase, restore/query, renewal, cancellation, refund/revocation, invalid token, duplicate RTDN, and out-of-order RTDN.
- [b] Verify production-vs-sandbox Apple signing configuration and notification endpoint registration.
- [b] Verify Google production Pub/Sub/RTDN topic and service-account configuration.
- [b] Reconcile the Google `p1m` billing period: the live catalog reports `P4W`, while the product contract and backend mapping intend monthly. Owner must either provision/activate a calendar-month base plan or explicitly approve four-week billing; then update the catalog contract, localized terms, tests, and backend billing-period mapping before a store-test build.
- [ ] Run a production no-entitlement query immediately before release.
- [b] Prefer one self-funded, refundable production smoke purchase per store to validate production notification plumbing. If the owner waives this, record the waiver and the residual risk; the release is not equivalent to full production plumbing verification.
- [b] Confirm store review metadata, privacy disclosures, subscription terms, restore-purchase affordance, and account deletion behavior.

Gate 3 acceptance:

- The complete matrix passes on both stores with captured transaction/event IDs and no secret values.
- Production notification configuration is proven, either by the refundable smoke transaction or by an explicit risk waiver.
- Production still has zero pre-existing entitlements before the IAP-only release.

Phase 3 progress:

- **2026-08-05 — local store-test preparation and live read-only Play catalog audit complete.** Added the dedicated EAS `store-test` build/submit profile: it is store-distributed through the `preview` environment, routes Android only to Play `internal`, pins the existing App Store Connect app ID, and remains distinct from the production submission profile. Four configuration tests and resolved EAS config for both platforms pass. Read-only EAS history proves the latest successful store binaries/submissions are version `1.1.1` from pre-IAP commit `455310298737cefae30f603b1c83abb2a6f26e4c`, so they are not Gate 3 evidence. A scoped Android Publisher read proves live products `p1m` (`P4W`, `P7D` grace) and `p2m` (`P1Y`, `P14D` grace) are active, but product listing localization is incomplete. `P4W` is not a calendar month and now has an explicit Gate 3 owner decision because the current product/backend contract calls this plan monthly. Added `docs/STORE_IAP_PHASE3_RUNBOOK.md` with the complete Apple/Google matrix, safe evidence fields, notification tests, metadata checklist, and owner handoff. Added an explicit read-only production entitlement audit; its current baseline reports 4 effective legacy accounts, all 4 in the controlled approval-test registry, 0 unclassified accounts, and no canonical store tables yet. An ephemeral PostgreSQL fixture also proves the post-migration branch distinguishes acknowledged production Apple/Google access, ignores an unacknowledged Google binding, separates sandbox rows, and reports an unclassified production entitlement. The temporary database was removed after the check. This is preparation evidence only and must be rerun immediately before release.
- **2026-08-05 — store screenshot copy guard complete.** The checked-in generator now validates every configured locale/slot before rendering and rejects missing/empty copy, Stripe/RevenueCat names, hard-coded currency prices, and unsupported trial/discount claims. The flashcard headline in all eight locale files now describes the implemented tap-to-flip interaction instead of claiming a nonexistent swipe gesture. Fable's implementation review confirmed that validation completes before workers can call an image provider and that the real config drives all locale/slot coverage, but found the flashcard image prompt still requested a swipe theme. The keyword was corrected to `tap`, a real-config regression assertion now requires that slot and excludes `swipe`, and the same Fable reconciliation returned `APPROVED`. Race-enabled package tests, package vet, the dry-run render plan, formatting, and diff checks pass without invoking an image provider.
- **2026-08-05 — Phase 3 local preparation complete; external gates explicit.** The mobile API resolver now fails closed on the `store-test` channel unless `EXPO_PUBLIC_API_URL` is an absolute nonproduction HTTPS URL; it rejects the production origin (including DNS-equivalent trailing-dot forms), local/loopback hosts, HTTP, malformed URLs, and the previous missing-variable fallback. The first combined Claude review attempt followed `fable` → `opus` → `sonnet`, with each model producing no verdict within its bounded window. A fresh narrowed attempt reset to Fable as required: separate Fable reviews approved the runtime/EAS isolation, the read-only entitlement audit, and the operator runbook after tracing their actual contracts. Twelve focused URL tests, four store-config tests, TypeScript, lint, formatting, shell syntax, JSON parsing, scoped diff checks, and resolved iOS/Android EAS store-test config pass. `[b] BLOCKED — owner action: deploy/authorize a separate sandbox-IAP API, add its HTTPS URL as EAS preview `EXPO_PUBLIC_API_URL`, authorize store-test builds/submissions, provide store-console and tester-device execution, complete missing Play product localizations, verify Apple products/V2 notification URLs and Google RTDN resources, and choose or waive the two refundable production smoke purchases. The current EAS preview environment has no API URL, so a store-test runtime deliberately cannot initialize and no build was started. Apple/TestFlight and Play matrix checkboxes remain blocked until their live evidence is captured.`
- **2026-08-05 — account-deletion compliance surface complete locally.** The responsive warm-editorial prototype under `docs/mockups/account-deletion/` covers direct in-app deletion, a no-app-access email request, data outcomes, and the required warning that account deletion does not cancel Apple/Google billing. Fable first required a visible email fallback, manual verification/policy-linked timing, and an H2 warning; all were reconciled and the prototype was approved. The localized production route, metadata, sitemap/footer discovery, and truthful two-step native confirmation are now implemented. Fable's implementation review then found that the public directions named Profile instead of the real Account row and that the success alert still overclaimed permanent erasure; both were corrected across every locale, the shipped-copy test and explicit final delete verb were added, and the same Fable reconciliation returned `APPROVED`. Production captures at 390×844 and 430×932 have no overflow, Axe Core 4.10.3 reports zero violations, the seven-locale web contract and focused mobile tests pass, and web/mobile lint, type checking, formatting, and production build pass. Store-console metadata and the broader profile-picture/third-party/backup deletion lifecycle remain separate sign-off gates.

## Phase 4 — IAP-only cutover and provider removal

- [ ] Drain or deliberately discard the legacy Stripe/RevenueCat River queues, then set `LEGACY_PROVIDER_WEBHOOKS_ENABLED=false` as part of the IAP-only release cutover, after the last RevenueCat-capable build stops selling and before treating native IAP as live. This removes legacy checkout, redirect, restore, webhooks, and workers together; any still-installed RevenueCat build loses restore at that moment, which is accepted because Phase 0 proved zero real paying users. Rollback requires an explicit config flip and deploy.
- [ ] Ship the IAP-only mobile build through the required store tracks/review process.
- [ ] Confirm entitlement grant, refresh, revoke, and restore in the live release before removing old code.
- [ ] Disable web checkout and return a deliberate “purchases are available in the mobile apps” state rather than a server error.
- [ ] Replace the provider-shaped subscription schema with Apple/Google fields and constraints. Preserve migration history; do not rewrite old migration files.
- [ ] Remove Stripe/RevenueCat provider enums, fields, SDKs, environment variables, webhook workers, checkout routes, and dead tests/docs.
- [ ] Validate that API and workers start, serve authenticated limits/status, and pass all checks with every Stripe/RevenueCat environment variable absent; this is Gate 4 evidence and depends on provider removal.
- [ ] Remove the temporary old-provider rollback flag after the first stable IAP release and successful monitoring window.
- [ ] Add post-cutover checks for entitlement counts, verification failures, notification lag, duplicate-event rate, acknowledgement failures, and support errors.

Gate 4 acceptance:

- `rg` finds no runtime Stripe or RevenueCat purchase/provider path, except historical migration documentation explicitly retained for migration history.
- Mobile, API, worker, and web checks pass with Stripe/RevenueCat environment variables absent.
- Web checkout attempts fail gracefully and cannot create a premium entitlement.
- A live Apple and Google entitlement can be refreshed and revoked without a provider-specific client SDK.

## Historical completion ledger — Revamp UX and analytics

The dated notes in this section preserve implementation evidence. Their broad checkboxes do not close any pending or partial row in the authoritative catalog above; future work is tracked only by milestone ID.

- [x] Correct the analytics foundation: retain id-only `identify`/sign-in coverage, remove PII from user properties/events, define anonymous-to-authenticated/reset handling, migrate onboarding and quiz activation events to the typed contract, capture privacy-safe notification opens, and define the dependent native-IAP funnel names/properties without mislabeling legacy Stripe/RevenueCat activity.
- [x] Decide whether quiz completion should return a session summary. If yes, add a versioned API contract with box transitions, counts, next due count, and error semantics; otherwise document the client query/aggregation contract explicitly.
- [x] Add due count to the intended progress-summary endpoint or remove it from the strategy’s requirement; do not infer it from reminder payloads.
- [x] Build a small coherent primitive layer for the revamp (Button, Card, Input, Sheet, typography/spacing tokens) on top of existing `components/ui` and shared styles.
- [x] Fix first-launch/auth routing, notification tap navigation, and flashcard end-of-list behavior as independently testable UX tasks.
- [x] Validate accessibility, reduced motion, loading/error states, offline/retry behavior, and small-screen layouts.

Historical revamp UX completion evidence:

- **2026-08-05 — adaptive flashcard reliability and motion pass complete.** The responsive prototype and decision evidence under `docs/mockups/flashcard-adaptive-practice/` define six distinct loading, processing, offline, error, empty, and ready states; a 220pt card floor with measured scrolling; large-text-safe two-line controls; screen-reader focus only after a direct flip; and interruption-safe flip, directional-settle, and press feedback with static reduced-motion fallbacks. Axe Core 4.10.3 reports zero prototype violations, the 360px specimen has no horizontal overflow, the compact specimen proves real vertical overflow, and forced reduced motion reduces transitions to an effectively static duration. Native implementation preserves generic shared-loading copy, bounds the current index synchronously during shrinking refetches, hides inactive card faces, separates pronunciation from flipping, localizes all new states in eight locales, and cancels five active animation values on unmount. Review restarted with `fable` (temporarily unavailable at its usage-limit 429); Claude `opus` rejected the first prototype with ten concrete design issues, approved the revised prototype, then found six native implementation/test issues plus one vacuous cleanup assertion. All were corrected and the same Opus thread returned `APPROVED`. Five focused suites pass 20 tests; the full repaired mobile harness passes 27 suites and 202 tests, and TypeScript, repository-wide lint, formatting/diff checks, and Android production export pass. The existing authenticated flashcard Maestro flow now exercises flip and flip-back semantics and is accepted by Maestro 2.4.0 through flow loading; live device execution remains environmentally blocked because no emulator/device is connected and the configured local API is unavailable, as already recorded for this workstream.
- **2026-08-05 — flashcard completion subtask complete.** The approved prototype and decision contract under `docs/mockups/flashcard-completion/` are now implemented natively: stable `Previous + Next/Finish wordlist` controls lead to an in-route, recoverable completion screen with honest unique-card views for the current visit, explicit review reset, saved-position handling, semantic progress, large-text scrolling, light/dark roles, focused completion announcement, and a Reanimated card-stack/bookmark entrance with a static reduced-motion state. Eight locales carry the new copy, and the completion illustration remains code-native because the surface is semantic and themeable. Native review restarted with `fable` (temporarily unavailable at its usage-limit 429), advanced after `opus` stalled, and used `sonnet`; shared persistence keys, singular/plural coverage, and duplicate-reset writes were corrected, two false findings were reconciled against the forwarded text ref and actual theme catalog, and the same Sonnet thread returned `APPROVED`. Twenty-two focused tests, TypeScript, repository-wide lint, formatting/diff checks, and Android production export pass. The full finite Jest run loads 110 passing tests; the same eight pre-existing suite-bootstrap failures remain because seven settings suites lack an Expo Notifications/`EXDevLauncher` mock and signup lacks `EXPO_PUBLIC_API_URL`. The authenticated Maestro flow is checked into `mobile/.maestro/flashcard-completion.yaml` and passes syntax validation; live execution is environmentally blocked because the configured local API at `10.0.2.2:3000` is absent and the repository compose stack requires unprovisioned external data volumes. The grouped first-launch, notification-routing, and flashcard-end milestone is therefore complete with that device-evidence limitation recorded for the reliability pass.
- **2026-08-05 — notification-tap navigation subtask complete.** Push opens now map only the backend's bounded reminder contract to owned routes: daily-practice reminders return to the dashboard, due-item reminders open the matching quiz only after validating a positive safe-integer wordlist ID, and unknown or malformed payloads remain non-navigating while retaining privacy-safe analytics. Cold-start destinations wait through the root gate and any sign-in/onboarding detour, then resume on an authenticated route; an already-open matching quiz is not stacked or remounted, and notification-only quizzes display the generic title instead of `undefined`. No visual prototype or new animation was needed because this subtask adds route semantics without a new screen. Review restarted with `fable` (temporarily unavailable at its usage-limit 429); `opus` then stalled without output and was treated as temporarily unavailable, so the protocol advanced to `sonnet`. Sonnet found the auth-detour destination loss, duplicate quiz stacking, and missing integration coverage; all were corrected, the complete validator and transition/malformed/replay tests were supplied to the same thread, and Sonnet returned `APPROVED`. Eighteen focused tests, TypeScript, repository-wide lint, formatting, and diff checks pass. The complete Jest command was also run: all 95 tests that loaded passed; the same eight pre-existing suites fail before execution because seven settings suites lack an Expo Notifications/`EXDevLauncher` mock and signup lacks `EXPO_PUBLIC_API_URL`.
- **2026-08-05 — first-launch/auth routing subtask complete.** Fresh installs now enter the three-step onboarding flow, returning users go directly to sign-in, authenticated users go to the dashboard even when an optional subscription query fails, and genuine unauthenticated session errors fail safely to sign-in. Onboarding completion is versioned in AsyncStorage; terminal exits clear the onboarding stack, while intentional sign-in-to-onboarding and onboarding-step transitions preserve Android back behavior. Session queries no longer run or revive stale identity without a secure token, and the authorization token is synchronously cached after its first secure read to avoid repeat bridge work. No visual prototype was needed because this subtask changes route/state semantics without introducing a new screen. Review restarted with `fable` (temporarily unavailable at its usage-limit 429); Claude `opus` identified stale stack entries, partial-error telemetry loss, weak tests, tokenless query failures, repeated SecureStore reads, and web-rendering risk, all were corrected, one disputed analytics point was reconciled against the approved activation contract, and the same thread returned `APPROVED`. Forty focused tests, TypeScript, repository-wide lint, formatting, diff checks, and the Android Maestro fresh-install/back/skip/relaunch flow pass. The complete Jest command was also run: all 80 tests that loaded passed; the same eight pre-existing suites fail before execution because seven settings suites lack an Expo Notifications/`EXDevLauncher` mock and signup lacks `EXPO_PUBLIC_API_URL`. Device validation required a temporary generated-Android runtime-version override because the ignored native project contains `file:fingerprint` while app config uses the app-version policy; the source was restored, and the native-build/IAP workstream must regenerate or reconcile that project before a store build.
- **2026-08-05 — privacy-safe analytics foundation complete.** Expanded the typed, versioned mobile analytics contract with id-only sign-in, onboarding, native-IAP funnel, notification-open, and restore events while retaining the existing signup, wordlist, and quiz activation names. The allowlist drops raw receipt/token/content fields, notification types collapse to a three-value catalog, authenticated identity uses only the positive numeric server ID, dashboard hydration retries identification, and central auth cleanup resets PostHog through a mounted bridge for manual sign-out, token expiry, account deletion, and storage-cleanup failures. Removed email/name from both PostHog and Sentry identity and disabled Sentry default PII collection. Onboarding now uses the validated contract; warm/cold notification responses emit one bounded event per request without title, body, wordlist name, or navigation payload, deduplicate launch/listener replay, and clear the consumed native response. Review restarted with `fable` (temporarily unavailable at its usage limit); Claude `opus` found the remaining Sentry PII, token-expiry identity-reset, and cold-start-test blockers, all three were corrected, and the same reviewer returned `APPROVED`. Fourteen focused tests, full TypeScript checking, repository-wide mobile lint, targeted formatting, and diff checks pass. The full Jest run still has the previously recorded harness failures before test execution: missing API environment in signup, the broken safe-area mock path in auth, and unmocked Expo Notifications in settings; all 31 tests that loaded passed, including the 14 analytics tests. The six native-IAP funnel call sites remain explicitly attached to the dependent paywall/purchase state item because emitting them from the current Stripe/RevenueCat screen would corrupt store-funnel data.
- **2026-08-05 — quiz completion remains a client-owned summary.** `docs/QUIZ_SESSION_SUMMARY_CONTRACT.md` records that the answer PATCH remains an empty per-answer `204`: the server receives no session ID, start, or completion signal, so returning cumulative counts or next-due state on every answer would invent a racy session boundary and add unrelated reads to the write path. The client owns interaction counts, duration, and success/cancellation analytics; durable box/progress data comes from explicit eventually-consistent queries, and next-due count remains the following progress-summary item. Review restarted with `fable` (temporarily unavailable at its usage limit); Claude `opus` found that analytics failure could silently roll back a Leitner update while still returning `204`, and that the image-error skip path could submit/count one displayed quiz twice. The transaction now returns analytics/commit failures, commits before detached snapshot work, and rolls back the Leitner transition on analytics failure; a process-local submission lock accepts exactly one answer or skip until the next quiz. Race-enabled unit tests lock the empty `204` and `400`/`404`/`500` errors, focused PostgreSQL integration proves `204` durability and analytics-failure rollback, and mobile unit tests prove single submission/reset behavior.
- **2026-08-05 — fresh per-wordlist due count contract complete.** Added `dueCount` to the API and mobile progress-summary contracts, sourced from the same definition-link, active-word, due-time, and temporary-skip predicates used by quiz selection rather than reminder payloads. Time-sensitive counts run fresh on every cached and uncached request while the existing slow progress aggregates remain under a versioned `v2` Redis key; only owned wordlists are merged into the response, missing counts become zero, and both old/new keys are invalidated after quiz writes. No UI prototype was needed because this item changes only the data contract; the dependent dashboard/card item owns its visual treatment. Review restarted with `fable` (temporarily unavailable at its usage limit); Claude `opus` found cached-time, stale-shape, and attribution-test blockers, all were corrected, and the reconciliation returned `APPROVED`. The full analytics PostgreSQL integration package passes, including eligible/future/skipped/learned/unlinked fixtures, two owned wordlists with counts `1` and `0`, and another user's excluded due row; repository/service compilation, Go formatting, vet, changed-file lint, diff checks, mobile type checking, and mobile lint also pass.
- **2026-08-05 — coherent mobile primitive layer complete.** Approved the warm editorial study-desk direction through the disposable responsive prototype and evidence under `docs/mockups/ui-primitives/`, then added shared `UiText`, `Button`, `Card`, `Input`, and controlled `Sheet` primitives with semantic light/dark roles, Space Mono Regular embedding, 44pt geometry, safe-area/keyboard handling, localized modal dismissal, Reanimated 4 interruption-safe motion, and reduced-motion/accessibility states. The font plugin requires a compatible native store build before OTA screen adoption; no existing screen was mass-migrated, so there is no affected production route for a Maestro flow in this foundation item. The implementation review restarted with `fable` (temporarily unavailable at its usage-limit 429), then Claude `opus` found twelve contrast, focus-order, test-harness, API, interaction, announcement, token, lifecycle-test, localization, and OTA-gating issues; all were corrected and the same `opus` thread returned `APPROVED`. Seven focused primitive tests and 20 affected auth tests pass, as do TypeScript, repository-wide mobile lint, formatting, Expo public-config generation, Android production export, and diff checks. The complete Jest command was run: all 62 tests that loaded passed; eight pre-existing suites still fail before test execution because seven settings suites lack an Expo Notifications/`EXDevLauncher` mock and signup lacks `EXPO_PUBLIC_API_URL`. Browser evidence covers light/dark, compact, sheet, focus, reduced-motion, and 360px no-overflow states; native screen migration and its scoped Maestro evidence remain with each owning UX item.

## Historical completion ledger — Severity-driven reliability triage

The dated notes in this section preserve implementation evidence. Remaining work, including the shutdown item below until reconciled, is governed by the authoritative milestone catalog and acceptance boundaries above.

- [x] Triage unscoped distractor and definition lookups first; verify language/wordlist/user boundaries.
- [x] Prove Apple/Google webhook idempotency is atomic, preserve Stripe's transactional event boundary, and make non-atomic RevenueCat ingress unreachable at the IAP-only cutover instead of rebuilding code scheduled for removal.
- [x] Make legacy provider configuration non-fatal and startup-validated: keep limits/status operational without provider credentials, require complete typed configuration while the rollback surface is enabled, and gate checkout/restore/workers together with legacy webhooks.
- [x] Correct the TTS language/voice contract and add a regression test.
- [x] Fix image-worker error-path formatting/strategy handling if still reachable.
- [x] Add timeouts and cancellation to outbound OpenAI clients where production work depends on them.
- [x] Reconcile documented and actual worker concurrency before increasing load.
- [x] Repair the one-command API integration harness so it uses the repository Go version, installs the current River schema after historical migrations, cleans current tables, reaches Compose services by container hostname, and runs the complete integration matrix with race detection.
- [x] Make the required full-tree API lint gate pass without globally disabling rules, lowering standards, or changing serialized and routing contracts.
- [x] **Fix before release:** make the production API SIGINT/SIGTERM path call bounded `srv.Shutdown` instead of waiting five seconds and exiting without graceful shutdown; add deterministic tests for production and non-production behavior before closing this item.
- [x] Move meaning-audio generation and storage outside the definition-fetch transaction, preserve idempotent completion/retry semantics, then measure queue depth and latency before considering a higher definition cap or worker DB pool.
- [x] Make account deletion atomic across the historical non-cascading error-report/wordlist relationships so a failed request cannot leave a partially erased account.
- [x] Reconcile the complete account-deletion data inventory before store sign-off: user-owned profile objects now have durable cleanup, PostHog/Sentry and backup behavior is described without unsupported synchronous-erasure claims, and live retention/alert configuration remains an explicit store-submission owner gate.
- [x] Remove direct identifiers and secrets from backend structured logs at both call-site and sink boundaries while preserving safe performance and correlation diagnostics.

Historical reliability completion evidence:

- **2026-08-06 — production API graceful shutdown complete.** SIGINT/SIGTERM now starts a bounded five-second production `http.Server.Shutdown`, development shutdown remains unbounded, listener failures return through `run()` so application cleanup defers execute, and a failed or timed-out drain force-closes remaining connections while preserving both errors when necessary. Deterministic tests cover production deadlines, development behavior, forced-close and joined-error paths, plus a real in-flight request that drains before process exit. Fable independently reviewed the implementation and returned `APPROVED`; exiting nonzero after a timed-out drain and not treating a second interrupt as an immediate force-exit are accepted non-blocking operational choices. `go test -race ./cmd/api -count=100`, `go vet ./cmd/api`, `make lint`, `make format-check`, and the exact containerized `make test` pass; its Compose project is cleaned up and the fresh coverage artifact was produced.
- **2026-08-06 — full-tree API lint gate complete.** The exact `make lint` initially exposed 121 uncapped findings that the changed-file workflow had hidden. Mechanical identifier, receiver, shadow, whitespace, constant, and dead-code cleanup now passes both the normal and uncapped lint commands without changing linter configuration. Persisted `cancelled`, correct Italian prompt text, httpexpect-owned bodies, four high-risk complexity sites, and legacy duplicate billing emails use narrow reasoned suppressions; all original JSON tags, River keys, route parameters, SQL/JSON paths, response keys, and observability strings were retained. Definition validation was split into focused helpers with race-enabled regression tests preserving non-verb and verb error ordering. Two intermediate full integration runs correctly exposed accidental string-casing drift; diff-guided restoration, focused real-PostgreSQL error-reporting/word/meaning-audio tests, and a final complete containerized matrix prove the corrected contracts. Fable independently audited all 121 categories and returned `APPROVE`; its required follow-up is the separately tracked production graceful-shutdown bug above, not an expansion of this lint-only implementation. `make lint`, uncapped lint, `make format-check`, `go vet ./...`, race-enabled unit and validator tests, exact `make test`, and diff checks pass. The validator test file lives in `internal/service`, so it is run explicitly and by the broader CI/internal-package command rather than by the narrowly scoped `make test-unit` target.
- **2026-08-06 — backend structured-log PII boundary complete.** The final local Maestro run exposed a normal login performance record containing the account email. Authentication, welcome/reset email, and legacy subscription error call sites now attach an internal user ID only after the user is known and never attach the raw email. A global immutable `slog.Handler` rebuilds records before the local/Sentry fan-out, filtering sensitive record attributes, pre-attached context, nested groups, and sensitive group names; the existing Sentry event/log scrubbers share normalization but retain their stricter query-fragment policy, while local logs preserve safe `db_query_ms` timing and remove exact query-string fields. Fable found that the first shared policy accidentally stripped `db_query_ms`; separate local/Sentry policies and regression coverage corrected it, and the same Fable reconciliation returned `APPROVED`. Race-enabled common privacy tests and the full API unit package pass; repository searches find no production raw-email log attribute.
- **2026-08-06 — account-deletion lifecycle complete locally.** Fable's design audit rejected a bespoke outbox in favor of River `InsertTx` and required stale-JWT/in-flight upload protection, terminal-failure operations, object-version deletion, and complete custom Sentry PII scrubbing. Account deletion now transactionally schedules a delayed, 25-attempt exact-prefix cleanup; the worker removes current MinIO objects and visible historical versions, while failed profile updates synchronously delete only the just-uploaded object and durably retry that exact object if storage is temporarily unavailable. API Sentry disables default PII, removes email/IP/query data at each custom context, and applies global event/log scrubbers. Public policy copy distinguishes active-database removal, retried profile-object cleanup, provider/backup retention, and legal holds without calling pseudonymous analytics anonymous or claiming synchronous complete erasure. The post-reset implementation review followed `fable` then `opus` five-minute latency fallbacks to `sonnet`; Sonnet found that the first compensation fallback could purge an active user's entire prefix, exact-object retry replaced that unsafe path, and the same Sonnet thread returned `APPROVED` after inspecting and rerunning the correction tests. Race-enabled API unit tests, formatting, changed-file lint, vet, web ESLint/Prettier, and diff checks pass. Real PostgreSQL proves job insertion/rollback is atomic with deletion; real versioned MinIO proves `users/42-` cleanup preserves `users/420-`; and forced post-upload database failure plus direct-delete failure tests prove both compensation branches. The temporary-reviewer protocol now keeps resettable limits active with yielded polling instead of prematurely blocking the goal. `[b] Owner action before store submission: complete the four live MinIO, backup, vendor-retention, and alert-routing sign-offs in docs/ACCOUNT_DELETION_OPERATIONS.md.`
- **2026-08-05 — atomic account-deletion fix complete.** The compliance audit proved the historical service performed independently committed cleanup before deleting the user, while `wordlists.user_id` does not cascade. Account deletion now performs explicit report cleanup, wordlist cascade cleanup, and user removal in one PostgreSQL transaction and propagates statement or commit failures. A focused real-PostgreSQL test proves complete user/wordlist/word/report deletion on success and complete rollback when the final user delete is forced to fail. Fable's implementation review confirmed the transaction, ordering, constructor wiring, nonexistent-user semantics, context cancellation, and rollback proof; it required correcting the schema comment to reflect that `error_reports.user_id` cascades since migration 000031 while `words.user_id` is the secondary non-cascading key swept through valid wordlist ownership. The correction was reconciled and the same Fable thread returned `APPROVED`. The full race-enabled integration matrix passes, as do race-enabled API unit tests, vet, formatting, changed-file lint, and diff checks. At this item's completion the one-command Compose wrapper still aborted when its one-shot bucket initializer exited, so the identical matrix was run against manually provisioned test-only services; the wrapper regression was subsequently fixed and revalidated through exact `make test` evidence on 2026-08-06. Public MinIO profile-picture objects and third-party/backup retention remain the following inventory item and continue to block full deletion/store-metadata sign-off.
- **2026-08-05/06 — API integration harness repair complete.** The Compose runner and standalone script now use Go 1.25.5, the harness applies River's bundled migrations after the immutable application migrations, and cleanup includes the current entitlement, notification, example-usage, legacy-event, and River tables while preserving River's migration ledger. The Compose test service uses portable named module caching; waits for PostgreSQL, Redis, MinIO, and bucket creation; addresses those services through the application's actual container-host variables; and runs integration packages serially with race detection and atomic coverage. The production-like Leitner fixture now has the minimum same-scope definitions required by the corrected distractor contract instead of depending on cross-scope options. A fresh Claude `fable` review traced migration ownership/idempotency, all live tables, environment precedence, readiness, isolation, flags, and fixture semantics, suggested replacing the inert MinIO override with `MINIO_HOST`, then returned `APPROVED` after that correction. Final validation later found that `up --exit-code-from main` still aborted when the successful one-shot bucket service exited first; the target now starts healthy dependencies, runs fail-fast idempotent bucket setup, runs the test container explicitly, verifies nonempty coverage, and always cleans up, including on INT/TERM. Fable reviewed and approved the corrected orchestration after its setup-failure and signal-cleanup findings were adopted. Exact `make test`, host-focused tests, every host integration package under `-race -count=1 -p=1`, Compose config validation, and focused container registration evidence pass.
- **2026-08-05 — meaning-audio transaction extraction complete.** Definition persistence now transactionally enqueues a dedicated per-definition meaning-audio job instead of calling OpenAI and MinIO while holding the transaction. River uniqueness hashes only the tagged definition ID, the worker exits when audio already exists, uses the deterministic object key, compares-and-sets an empty URL, retries transient eligibility/provider/storage/persistence failures, snoozes rate limits, and cancels permanent business, billing, empty-content, and not-found failures. Meaning-audio enqueue is deliberately a required outbox operation: failure rolls back the definition transaction rather than committing quiz content that can never gain audio, with an explicit regression test for propagation. Real PostgreSQL evidence proves transaction rollback, per-definition uniqueness despite different word/user arguments, concurrent compare-and-set behavior, and typed not-found handling. Definition and meaning-audio queues are capped at 2 each, preserving the prior four-worker provider budget and aggregate totals of 189/179 while the database pool remains 10. Review restarted with `fable` (usage-limit 429), `opus` timed out, and `sonnet` found the initial diff-context, queue-budget, caller-error, eligibility-classification, and enqueue-policy gaps; all were corrected or reconciled, and the same Sonnet thread returned `APPROVED`. Race-enabled service and focused PostgreSQL tests, API build/vet, changed-file lint, formatting, and diff checks pass. Production queue depth and latency remain an observation gate before any concurrency increase.
- **2026-08-05 — worker concurrency reconciled and contained.** The stale 226-slot audit figure was replaced with the actual configuration: previously 235 nominal River slots against a 10-connection worker pool. MaxWorkers is a scheduling ceiling rather than a connection reservation, but definition jobs can hold a database transaction across bounded meaning-audio generation and storage; their queue is therefore reduced from 50 to 4 so it cannot consume the full pool and at least six connections remain available to other queues. The worker pool stays at 10 because production PostgreSQL capacity, worker replica count, and connection headroom have not yet been measured; no unsupported pool or load increase was made. Queue construction now omits the two five-slot Stripe/RevenueCat queues when the rollback surface is disabled, producing deterministic totals of 189 with legacy rollback enabled and 179 in IAP-only mode. Tests assert every queue cap, both totals, legacy gating, and the definition-cap-versus-pool safety invariant. Review restarted with `fable` (temporarily unavailable at its usage-limit 429), then `opus` produced no output before the bounded timeout, so `sonnet` completed the audit. Sonnet first identified the long transaction as the material risk, then approved the smaller cap-based containment after rejecting an evidence-free pool increase; it required the per-queue and invariant tests plus explicit documentation of the throughput tradeoff and durable transaction refactor. Focused race-enabled service tests pass. The cap can increase new-word enrichment latency under backlog, so the next reliability item must extract meaning-audio I/O from the transaction and observe queue depth/job latency before any concurrency increase.
- **2026-08-05 — production OpenAI calls bounded and cancellable.** Definition, image, speech, and Realtime-token clients now share an injectable transport with a 150-second ceiling, operation-specific 90/120/30/10-second deadlines, caller-context propagation, existing response decoding, and unchanged authenticated request shapes. Definition, image, text-to-speech, and example-audio workers declare explicit 6/4/2/10-minute River budgets; worker cancellation now returns an error instead of silently completing, and definition persistence uses explicit commit-error propagation plus rollback so cancellation cannot report success after losing the transaction. Realtime token creation moved from the generic two-second authenticated group to an otherwise identical guarded group with a 15-second route budget around the ten-second provider call. Review restarted with `fable` (temporarily unavailable at its usage-limit 429); Claude `opus` found unbounded slot starvation, held transactions, dead handler work, then identified cancellation-success, transaction-rollback, transport-coverage, and timeout-headroom gaps in the first implementation pass. All material findings were corrected, and the same `opus` thread returned `APPROVED`. Race-enabled OpenAI, service, and API unit suites cover caller cancellation, operation deadlines, transport ceiling, all four paths, authorization, audio decoding, and worker budgets; the focused race-enabled PostgreSQL example worker test proves both language passthrough and mid-call cancellation. API build, vet, formatting, changed-file lint, and diff checks pass. Meaning-audio generation still occurs inside the definition transaction but is now bounded; moving it to a separate idempotent job remains a distinct architecture improvement.
- **2026-08-05 — reachable image-worker prompt and resolution defects fixed.** Every supported image prompt previously supplied four arguments to three format verbs, appending a `%!(EXTRA string=...)` artifact to both the paid OpenAI request and persisted prompt. User-submitted missing/unrelated-image reports also completed generation, upload, and persistence before invoking a zero-value Leitner strategy, panicking and allowing River to repeat the paid pipeline up to its retry limit without resolving the report. Prompt formatting now uses exactly the sentence, token, and meaning arguments; the worker receives the real shared Leitner strategy from the composition root, and missing-strategy misconfiguration returns `JobCancelError` to prevent paid retries. Review restarted with `fable` (temporarily unavailable at its usage-limit 429); Claude `opus` confirmed both paths are reachable, verified the wiring and retry semantics, and returned `APPROVED`. Race-enabled in-package tests cover all seven prompt languages, unsupported-language failure, absence of formatting leakage, panic prevention, and cancellation rather than retry; API build, vet, formatting, changed-file lint, and diff checks pass. A transient database failure after successful generation can still cause regeneration in both image and TTS workers; solving that requires generation idempotency or a separate resolution policy and is outside this narrowly scoped defect.
- **2026-08-05 — TTS language/voice contract corrected.** Example-sentence audio previously mapped a wordlist language such as `es` to the voice name `nova`, then passed that voice into `GenerateAudio`'s language-code parameter; the OpenAI boundary mapped it a second time and silently fell back to `alloy` with generic English instructions. The worker now passes the original wordlist language, the duplicate service-level voice table is deleted, and `internal/openai` is the sole owner of TTS voice/instruction selection. Language matching trims, lowercases, and uses the base subtag so regional Portuguese resolves correctly, while unknown codes retain the existing safe generic fallback. Review restarted with `fable` (temporarily unavailable at its usage-limit 429), then Claude `opus` confirmed the exact double-mapping bug, approved the consolidated contract, and returned `APPROVED` after verifying all callers and tests. Race-enabled OpenAI and API unit suites pass; a focused race-enabled PostgreSQL worker test proves a Spanish wordlist reaches the generator as `es` rather than `nova`. API build, vet, formatting, changed-file lint, and diff checks pass. Realtime voice selection and definition-language exact matching remain deliberately unchanged and belong to their own contract work; this item changes only `/v1/audio/speech` TTS behavior.
- **2026-08-05 — legacy provider startup coupling removed.** Replaced six Stripe/RevenueCat constructor panics and the RevenueCat webhook's request-time environment panic with one production-explicit, typed startup contract covering all required Stripe keys, price IDs, URLs, RevenueCat API authorization, and webhook authorization without logging secret values. The core subscription service now receives optional Stripe configuration and keeps local/effective entitlement status plus free-tier limits available with no legacy credentials; disabled optimistic RevenueCat checks cannot dereference a missing client. The same rollback switch now removes Stripe checkout, checkout redirect, RevenueCat restore, both legacy webhooks, and both legacy River workers together, while leaving Apple/Google webhooks and native-IAP routes independent. Existing enabled environments remain fail-closed and behavior-compatible when fully configured, the test-environment template carries the complete contract, webhook authorization compares in constant time, and Phase 4 requires draining legacy queues before disabling their workers. Review restarted with `fable` (temporarily unavailable at its usage-limit 429); Claude `opus` found the six startup panics, wider limit-service coupling, ungated routes/workers, then identified test-template, behavioral-evidence, queue-drain, and documentation gaps in the first implementation pass. All findings were corrected, and the same `opus` thread returned `APPROVED`. The complete race-enabled API unit package passes; focused race-enabled PostgreSQL tests prove disabled-surface composition-root and worker construction, second-wordlist and eleventh-word enforcement through the optimistic nil-client path, local subscription status, and enabled-mode named startup failure. API build, vet, formatting, changed-file lint, and diff checks pass. A broader legacy-webhook integration invocation still hits the independently documented harness defect where migration `000052` removes `river_job` while old webhook tests expect that table; the focused startup evidence does not depend on that missing table. Phase 4 still owns the final credential-free API/worker boot proof after provider code and dependencies are deleted.
- **2026-08-05 — webhook atomicity and legacy-ingress cutover control complete.** Apple and Google notification processing was audited against the shared provider inbox: lease-token ownership, row locking, entitlement mutation, and terminal inbox result commit atomically, with concurrent-claim and rollback integration evidence already in the suite. Stripe's subscription mutation and unique external event marker share one transaction, while RevenueCat's check/process/store sequence remains non-atomic and is deliberately not being rebuilt before removal. Added production-explicit `LEGACY_PROVIDER_WEBHOOKS_ENABLED` configuration and route-registration gating so both Stripe and RevenueCat webhook endpoints are absent together at the IAP-only cutover, remain default-enabled in development/test, and can be restored only by an explicit config change and deploy; Apple/Google webhooks and native IAP routes are outside this gate, while legacy checkout and restore were folded into it by the immediately following startup-coupling item. Named transaction results now propagate deferred commit failures from legacy create/update operations, allowing River retries instead of reporting lost commits as success. Review began with `fable` (temporarily unavailable at its usage-limit 429); Claude `opus` recommended reachability gating over a RevenueCat retrofit, then verified the implementation and returned `APPROVED`. The complete race-enabled API unit package and the focused race-enabled PostgreSQL deferred-constraint test pass; the latter proves create rollback, update rollback, and non-nil commit errors. API build, vet, formatting, changed-file lint, and repository diff checks pass. Before deploying this code, operators must explicitly provision the production flag as `true`; the Phase 4 cutover owns changing it to `false`, the pre-flip zero-real-legacy-entitlement query, legacy duplicate/out-of-order characterization, and rollback drill. This completion means “atomic native providers plus legacy ingress made unreachable at cutover,” not “RevenueCat made atomically idempotent.”
- **2026-08-05 — definition and quiz-distractor boundaries complete.** Single-word definition reads now validate positive path IDs and require the authenticated user, URL wordlist, and word to match; an owned word cannot be fetched through another owned wordlist path, and empty authorized results serialize consistently as `[]`. Token and meaning distractors are restricted to the authenticated user's current wordlist, language, normalized part of speech, and non-target word/answer, with case-insensitive compatibility for legacy normalized values and true token deduplication before random selection. Every option-bearing quiz type receives the same explicit scope. Multiple-choice questions require two scoped distractors: unfiltered or explicitly write-enabled sessions fall back safely to write-in practice, filtered sessions return the existing client-handled `no_matching_quiz_types` 409, and filtered candidate selection skips a thin-POS head item to avoid wedging later compatible due items; the final guard remains for races. No UI prototype was needed because this item changes only authenticated repository/service behavior and preserves the mobile response shapes. Review restarted with `fable` (temporarily unavailable at its usage-limit 429); Claude `opus` verified the boundary predicates, found the one-option quiz, legacy casing, scope-wiring, deduplication, and filtered-queue risks, and approved after all were corrected and reconciled in the same thread. Four race-enabled PostgreSQL `words` tests cover cross-user, cross-list, cross-language, cross-POS, legacy casing, duplicate token/meaning, and single/batch route behavior; two root PostgreSQL tests prove distinct user/wordlist scope wiring, candidate skipping, three-option output, safe write fallback, and filtered 409. Race-enabled API unit tests, package compilation, Go formatting, changed-file lint, vet/build review checks, and staged diff checks pass. Filtered mode performs at most two bounded, indexed distractor lookups per examined candidate (maximum 50 candidates); monitor filtered-quiz latency before considering a set-based optimization.

## Explicitly removed from this revamp

- [ ] Existing subscriber migration, grandfathering, refunds, or billing-history preservation.
- [ ] Stripe fee optimization, RevenueCat entitlement migration, or current-payer churn work.
- [ ] Web checkout growth work that assumes Stripe remains.
- [ ] A large unrelated rewrite of content generation, worker topology, or analytics infrastructure without a launch-critical finding.

## Required validation commands

Run after implementation, from the relevant directory:

```text
bash -n docs/poll-claude-review.sh
docs/validate-mobile-revamp-plan.sh
cd api && make test && make lint && make format-check
cd mobile && npm run lint && npm run typecheck && npm test
cd web && npm run lint && npm run format:check && npm run build
```

Also run targeted tests for store verification, notification redelivery/idempotency, entitlement refresh, web checkout-disabled behavior, and absent Stripe/RevenueCat configuration.

## External implementation references

- [Apple App Store Server API](https://developer.apple.com/documentation/appstoreserverapi/)
- [Apple App Store Server Notifications](https://developer.apple.com/documentation/AppStoreServerNotifications/receiving-app-store-server-notifications)
- [Google Play Billing integration](https://developer.android.com/google/play/billing/integrate.html)
- [Google Play billing security](https://developer.android.com/google/play/billing/security)
- [Expo development builds](https://docs.expo.dev/develop/development-builds/use-development-builds/)
