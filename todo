- Generate quiz
    - once a word is created:
        [x] fetch and save definitions
        [x] insert in the quiz engine
        [x] save result
        [x] increase box number automatically
        [x] load options from the db
        [x] load index of right option
        [x] implement complete sentence quiz
        [x] write script to load definitions from Wiktionary json file
        [x] write Wiktionary definer in definitions/wiktionary package
        [x] use openai to only get examples of usage of words
        [x] use openai only as a fallback when the wiktionary strategy fails
        [x] save words in lowercase
        [x] remove calls to log.Fatal in api/internal/definitions/openai/service.go
        [x] handle the case of form_of records in wiktionary
        [ ] quiz options are been returned more than once 
        [ ] timeout requests if db takes longer to respond in the api
        [ ] add sentry   
        [ ] use structured logging 

- Mobile
    [ok] signup
        [x] store cookie via SecureStore
        [x] externalize server ip address
    [ok] login
    [ ] dashboard
        [x] list wordlists
        [x] add word
        [ ] toolbar button to create a new wordlist
        [ ] display custom screen if user doesn't have wordlists
        [ ] clicking on a word should open a modal defining the clicked word
        [ ] display word status: pending, error, no meaning found, etc
        [ ] review mode: display words so that user can learn their meaning and usage
        [ ] (v2) display how strong is the definition of a word for an user 
    [ ] quiz
        [ ] use challenge instead of quiz
        [ ] make button read if clicking the wrong option
        [ ] make button green if clicking the wrong option
        [ ] When replacing option, be aware of suffixes 
        [ ] button click effect with depth effects
        [x] refactoring
        [x] check keys of children components
    [ ] add sentry    
    [ ] recover password in the login screen
    [ ] logout

- deploy
    [ ] create a docker image
    [ ] deploy api to heroku
    [ ] continuos deployment from Github
    [ ] mobile deployment


- automated tests
- [REFACTORING] When saving word, check settings to decide which definer should be use for definitions.FetchAndSave
- When saving new word, move the goroutine calling FetchAndSave and IncludeDefinitions to a processing queue 
- customize quiz algorithm based on user settings ( api/internal/quizzes/handlers.go )
- Reuse entries already fetched from wiktionary or chatgpt
- configurar honnef.co/go/tools/cmd/staticcheck
- configurar go vet as a pre-commit hook
- configure govulncheck golang.org/x/vuln/cmd/govulncheck@latest